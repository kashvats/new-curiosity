You are continuing the existing local AI coding assistant project.

PHASE 8 ONLY: Add a LangGraph planner that breaks large user requests into safe implementation phases.

Do not add coding agent yet.
Do not apply file changes yet.
Do not add reviewer agent yet.
Do not add web search yet.
Do not add browser control yet.
Do not modify project files automatically.
Do not rewrite frontend except for a simple planner UI.

Your only goal in this phase:
Create a planner endpoint that takes a large user request and returns a structured phase-by-phase plan that Qwen2.5-Coder can handle one phase at a time.

IMPORTANT PROTECTION RULES:
- Do not remove any previous features.
- Do not refactor unrelated files.
- If an error is found, touch only the exact file related to that error.
- Do not regenerate the whole project.
- Do not delete existing endpoints.
- Do not change existing API contracts unless absolutely required.
- Planner must not edit files.
- Planner must not run terminal commands.
- Planner must only produce a plan.

Existing features that must continue working:
- All Phase 1 to Phase 7 endpoints and frontend sections.

Use:
- LangGraph for planner workflow.
- Planner model from env: PLANNER_MODEL
- Default planner model: qwen3:4b
- Ollama base URL from env: OLLAMA_BASE_URL

Update .env.example:

PLANNER_MODEL=qwen3:4b
MAX_PLAN_PHASES=12

Backend requirements:

1. Update requirements.txt.

Add:
- langgraph
- langchain
- langchain-community

Do not remove existing packages.

2. Add planner.py.

Create backend/app/planner.py.

It should include:
- PlannerState TypedDict
- build_planner_prompt(user_request: str) -> str
- call_planner_model(prompt: str) -> dict
- parse_planner_json(raw_text: str) -> dict
- create_plan(user_request: str) -> dict

Planner output must be strict JSON:

{
  "goal": "string",
  "summary": "string",
  "recommended_stack": [
    "string"
  ],
  "phases": [
    {
      "id": 1,
      "title": "string",
      "description": "string",
      "files_likely_needed": [
        "string"
      ],
      "dependencies": [
        1
      ],
      "success_criteria": [
        "string"
      ],
      "allowed_actions": [
        "create_file",
        "modify_file",
        "read_file"
      ],
      "forbidden_actions": [
        "delete_unrelated_files",
        "rewrite_unrelated_modules",
        "remove_existing_features"
      ]
    }
  ],
  "risks": [
    "string"
  ],
  "next_phase_prompt": "string"
}

Rules for planner:
- Break large work into small safe phases.
- Each phase must have one clear goal.
- Each phase must preserve existing features.
- Each phase must include success criteria.
- Each phase must list likely files.
- Each phase must forbid unrelated rewrites.
- Do not create more than MAX_PLAN_PHASES unless absolutely necessary.
- Prefer incremental implementation.
- Prefer minimal file changes.
- Include a ready-to-copy prompt for only Phase 1 in next_phase_prompt.

3. Use LangGraph.

Create a simple LangGraph workflow:

START
→ planner_node
→ validate_json_node
→ END

planner_node:
- Calls Ollama planner model.
- Stores raw response.

validate_json_node:
- Extracts JSON from model response.
- Validates required keys.
- If invalid JSON, attempts one repair call or local cleanup.
- If still invalid, returns clear error.

Do not make a complex multi-agent graph yet.
This phase is only planner.

4. Add endpoint:

POST /agent/plan

Request:

{
  "request": "Build a dashboard with document upload and RAG search"
}

Response success:

{
  "status": "ok",
  "model": "qwen3:4b",
  "plan": {
    "goal": "...",
    "summary": "...",
    "recommended_stack": [],
    "phases": [],
    "risks": [],
    "next_phase_prompt": "..."
  }
}

Response error:

{
  "status": "error",
  "model": "qwen3:4b",
  "message": "..."
}

5. Add endpoint:

GET /agent/planner/health

Behavior:
- Check Ollama availability.
- Check planner model setting.
- If possible, check model exists in Ollama tags.
- Return:

{
  "status": "ok",
  "model": "qwen3:4b"
}

If unavailable:

{
  "status": "error",
  "model": "qwen3:4b",
  "message": "..."
}

Do not crash if Ollama is offline.

6. Optional local fallback.

If planner model fails or returns invalid JSON:
- Return a safe simple plan with 3-5 phases based on the request.
- Mark response with:
  "fallback_used": true

Do not hallucinate that model succeeded.

Frontend requirements:

Add simple Planner section:

- Textarea: "Describe what you want to build"
- Button: "Create Plan"
- Loading state
- Error display
- Show:
  - goal
  - summary
  - recommended stack
  - phases as cards/table
  - success criteria
  - likely files
  - next_phase_prompt in copyable textarea

Also show planner health:
- /agent/planner/health

Do not redesign full UI.
Do not add routing.
Do not remove previous UI sections.

Code quality requirements:

Backend:
- Keep planner logic in planner.py
- Keep route logic in a new agent.py router if cleaner
- Keep existing chat/rag/search logic unchanged
- Use Pydantic models if project already uses them
- Keep functions small and readable
- Do not log huge prompts
- Do not store plans in database yet

Frontend:
- Keep App.jsx readable
- Use helper functions
- Preserve all existing features
- Show loading and error states

Performance requirements:
- My machine: GTX 1650 4GB VRAM, 32GB RAM, Ryzen 5 5600H
- Planner model should be small.
- Default planner model qwen3:4b.
- Do not run planner and chat model at same time.
- No parallel agent calls.

Success criteria:

1. All previous phase features still work.
2. GET /agent/planner/health works.
3. POST /agent/plan returns structured JSON plan.
4. Planner never edits files.
5. Planner creates small safe phases.
6. Frontend can generate and display a plan.
7. No coding agent, reviewer, file editing, web search, or browser control is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 9.

--------------------------------------------------------------------------------------------------------------------------

You are continuing the existing local AI coding assistant project.

PHASE 9 ONLY: Add a coding agent that drafts file changes but does NOT apply them automatically.

Do not add reviewer agent yet.
Do not apply file changes yet.
Do not run generated code automatically.
Do not add web search yet.
Do not add browser control yet.
Do not modify project files based on the agent output.
Do not rewrite frontend except for a simple coding-agent UI.

Your only goal in this phase:
Create an endpoint where the user provides:
1. A phase/task description
2. Optional selected file paths
3. Optional extra context

The coding agent should use Qwen2.5-Coder and return structured proposed file changes as JSON.

IMPORTANT PROTECTION RULES:
- Do not remove any previous features.
- Do not refactor unrelated files.
- If an error is found, touch only the exact file related to that error.
- Do not regenerate the whole project.
- Do not delete existing endpoints.
- Do not change existing API contracts unless absolutely required.
- Coding agent must not write to disk.
- Coding agent must not delete files.
- Coding agent must not run terminal commands.
- Coding agent must only return proposed changes.

Existing features that must continue working:
- All Phase 1 to Phase 8 endpoints and frontend sections.

Use:
- Coder model from env: CODER_MODEL
- Default coder model: qwen2.5-coder:7b
- Ollama base URL from env: OLLAMA_BASE_URL

Update .env.example:

CODER_MODEL=qwen2.5-coder:7b
MAX_CODE_CONTEXT_FILES=8
MAX_CODE_FILE_CHARS=12000

Backend requirements:

1. Add coder.py.

Create backend/app/coder.py.

It should include:
- build_coder_prompt(task, files, extra_context) -> str
- call_coder_model(prompt) -> str
- extract_json_from_response(raw_text) -> dict
- draft_code_changes(task, file_paths, extra_context) -> dict

2. Add safe file reading.

The coding agent may read selected files only.

Rules:
- Read only files explicitly provided in request.
- Do not scan entire project automatically.
- Do not read files outside allowed workspace root.
- Block path traversal like ../../secret.txt
- Limit files to MAX_CODE_CONTEXT_FILES.
- Limit each file to MAX_CODE_FILE_CHARS.
- If file is too large, include beginning and end with a truncation note.
- If file does not exist, include that in context as "missing file".

Define workspace root from env:

WORKSPACE_ROOT=/workspace/project

For Docker, mount project workspace if needed:

./workspace:/workspace/project

Do not modify unrelated Docker volumes.

3. Coding agent output format.

The model must return strict JSON:

{
  "summary": "string",
  "task_understood": "string",
  "files_read": [
    "path"
  ],
  "proposed_changes": [
    {
      "path": "relative/path/to/file",
      "action": "create|modify",
      "reason": "why this file needs change",
      "content": "complete proposed file content"
    }
  ],
  "commands_to_run_manually": [
    "string"
  ],
  "warnings": [
    "string"
  ]
}

Important:
- No markdown outside JSON.
- No comments like "here is the JSON".
- proposed_changes must contain complete file content, not vague instructions.
- For modify action, preserve existing working behavior.
- Do not remove existing features unless task explicitly asks.
- Do not touch unrelated files.
- Do not propose deleting files in this phase.

4. Coding prompt rules.

The prompt must tell Qwen2.5-Coder:

- You are editing an existing project.
- Only work on the current task.
- Use provided files as source of truth.
- Preserve existing features.
- Do not rewrite unrelated modules.
- If a bug is in one file, modify only that file.
- If you need more files, say which files are needed instead of guessing.
- Return strict JSON only.
- Do not apply changes.

5. Add endpoint:

POST /agent/code

Request:

{
  "task": "Add a loading spinner to the upload button",
  "file_paths": [
    "frontend/src/App.jsx"
  ],
  "extra_context": "Do not change backend."
}

Response success:

{
  "status": "ok",
  "model": "qwen2.5-coder:7b",
  "result": {
    "summary": "...",
    "task_understood": "...",
    "files_read": [],
    "proposed_changes": [],
    "commands_to_run_manually": [],
    "warnings": []
  }
}

Response error:

{
  "status": "error",
  "model": "qwen2.5-coder:7b",
  "message": "..."
}

6. Add endpoint:

GET /agent/code/health

Behavior:
- Check Ollama availability.
- Check coder model setting.
- If possible, check model exists in Ollama tags.
- Return:

{
  "status": "ok",
  "model": "qwen2.5-coder:7b"
}

If unavailable:

{
  "status": "error",
  "model": "qwen2.5-coder:7b",
  "message": "..."
}

Do not crash if Ollama is offline.

7. Add optional endpoint:

GET /workspace/files

Purpose:
List allowed workspace files for user selection.

Rules:
- Only list files under WORKSPACE_ROOT.
- Exclude:
  - node_modules
  - .git
  - dist
  - build
  - __pycache__
  - .venv
  - data
  - qdrant_storage
- Return relative file paths only.
- Do not return file contents.

Response:

{
  "root": "/workspace/project",
  "files": [
    "backend/app/main.py",
    "frontend/src/App.jsx"
  ]
}

Frontend requirements:

Add simple Coding Agent section:

- Textarea for task
- Text input/textarea for file paths, one per line
- Textarea for extra context
- Button: "Draft Changes"
- Loading state
- Error display
- Show:
  - summary
  - files read
  - proposed changes
  - path
  - action
  - reason
  - code block with proposed content
  - commands to run manually
  - warnings

Also show code agent health:
- /agent/code/health

Important:
- Do not apply changes from frontend.
- This phase is preview/draft only.
- No "Apply" button yet.

Do not redesign full UI.
Do not add routing.
Do not remove previous UI sections.

Code quality requirements:

Backend:
- Keep coding-agent logic in coder.py.
- Keep route logic in existing agent.py if it exists, or create it if not.
- Keep planner logic unchanged.
- Use Pydantic models if project already uses models.py.
- Use pathlib for safe paths.
- Keep functions small.
- Do not log full file contents.
- Do not store drafts in database yet.

Frontend:
- Keep App.jsx readable.
- Use helper functions.
- Preserve all existing features.
- Show loading and error states.

Performance requirements:
- My machine: GTX 1650 4GB VRAM, 32GB RAM, Ryzen 5 5600H.
- Do not send too many files.
- Default max files: 8.
- Limit file size sent to model.
- Do not run planner and coder at same time.
- No parallel agent calls.

Success criteria:

1. All previous phase features still work.
2. GET /agent/code/health works.
3. GET /workspace/files lists allowed files only.
4. POST /agent/code returns proposed file changes as strict JSON.
5. Coding agent never writes to disk.
6. Coding agent never deletes files.
7. Frontend can draft and display proposed changes.
8. No reviewer, apply-changes, web search, or browser control is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 10.

--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 11 ONLY: Add safe apply-changes support for reviewed and approved file changes.

Do not add web search yet.
Do not add browser control yet.
Do not auto-run terminal commands.
Do not auto-fix code.
Do not apply unreviewed changes.
Do not rewrite frontend except for a simple apply-changes UI.

Your only goal in this phase:
Create a safe endpoint that applies proposed file changes only after:
1. The user provides proposed changes.
2. The user provides a review result.
3. The review says approved=true and safe_to_apply=true.
4. The request includes confirm=true.
5. The backend creates backups before writing files.

IMPORTANT PROTECTION RULES:
- Do not remove any previous features.
- Do not refactor unrelated files.
- If an error is found, touch only the exact file related to that error.
- Do not regenerate the whole project.
- Do not delete existing endpoints.
- Do not change existing API contracts unless absolutely required.
- Apply endpoint must only write files listed in proposed_changes.
- Apply endpoint must never delete files.
- Apply endpoint must never run commands.
- Apply endpoint must never modify files outside WORKSPACE_ROOT.

Existing features that must continue working:
- All Phase 1 to Phase 10 endpoints and frontend sections.

Use:
- Existing WORKSPACE_ROOT from Phase 9.
- Backup directory from env: BACKUP_DIR
- Default backup directory: /workspace/backups

Update .env.example:

BACKUP_DIR=/workspace/backups

Docker requirement:
If needed, mount:
./backups:/workspace/backups

Do not break existing Docker volumes.

Backend requirements:

1. Add apply_changes.py.

Create backend/app/apply_changes.py.

It should include:
- validate_apply_request(...)
- validate_safe_path(relative_path: str) -> Path
- create_file_backup(target_path: Path) -> dict
- generate_unified_diff(old_content: str, new_content: str, path: str) -> str
- apply_file_change(change: dict) -> dict
- apply_reviewed_changes(...) -> dict

2. Safe path rules.

Only allow relative paths under WORKSPACE_ROOT.

Reject:
- absolute paths
- path traversal like ../../file
- paths outside WORKSPACE_ROOT
- empty paths
- Windows drive paths like C:\...
- hidden dangerous paths unless explicitly allowed

Never allow deletion.

Allowed actions:
- create
- modify

Rejected actions:
- delete
- remove
- rename
- move
- chmod
- command

3. Review validation.

The apply endpoint must require review data.

It can only proceed when:

review.approved == true
review.safe_to_apply == true
review.risk_level is "low" or "medium"
confirm == true

Reject if:
- review is missing
- approved is false
- safe_to_apply is false
- risk_level is high
- confirm is false
- proposed changes contain files not present in review.files_reviewed, unless action is create and review approved it
- proposed changes include rejected files
- proposed changes contain protected files and review did not explicitly approve them

Protected files:
- docker-compose.yml
- backend/Dockerfile
- frontend/Dockerfile
- .env
- .env.example

4. Backup behavior.

Before modifying an existing file:
- Create backup in BACKUP_DIR with timestamp.
- Preserve relative path structure.

Example:

Original:
frontend/src/App.jsx

Backup:
backups/2026-xx-xxTxx-xx-xx/frontend/src/App.jsx

For new files:
- No original backup needed.
- Still log that it was newly created.

If backup fails:
- Do not apply changes.
- Return error.

5. Diff behavior.

Before applying:
- Generate unified diff for each file.
- Include diffs in response.
- If dry_run=true, only return diffs and do not write files.

6. Add endpoint:

POST /agent/apply

Request:

{
  "task": "Add loading spinner",
  "proposed_changes": [
    {
      "path": "frontend/src/App.jsx",
      "action": "modify",
      "reason": "Add loading state",
      "content": "complete file content"
    }
  ],
  "review": {
    "approved": true,
    "safe_to_apply": true,
    "risk_level": "low",
    "files_reviewed": [
      "frontend/src/App.jsx"
    ],
    "rejected_files": []
  },
  "confirm": true,
  "dry_run": false
}

Response success:

{
  "status": "applied",
  "applied_files": [
    {
      "path": "frontend/src/App.jsx",
      "action": "modify",
      "backup_path": "/workspace/backups/...",
      "diff": "..."
    }
  ],
  "skipped_files": [],
  "warnings": []
}

Dry run response:

{
  "status": "dry_run",
  "applied_files": [],
  "planned_files": [
    {
      "path": "frontend/src/App.jsx",
      "action": "modify",
      "diff": "..."
    }
  ],
  "warnings": []
}

Rejected response:

{
  "status": "rejected",
  "message": "...",
  "reasons": [
    "Review is not approved."
  ]
}

7. Add endpoint:

GET /agent/apply/health

Response:

{
  "status": "ok",
  "workspace_root": "...",
  "backup_dir": "..."
}

If paths are invalid:

{
  "status": "error",
  "message": "..."
}

8. Add optional endpoint:

GET /agent/backups

Return backup folders/files:

{
  "backup_dir": "...",
  "backups": [
    {
      "timestamp": "...",
      "files": [
        "frontend/src/App.jsx"
      ]
    }
  ]
}

Do not add restore yet.
Restore can be later phase if needed.

Frontend requirements:

Add simple Apply Changes section:

- Textarea for proposed changes JSON
- Textarea for review JSON
- Checkbox: confirm apply
- Checkbox: dry run
- Button: "Preview / Apply Changes"
- Loading state
- Error display
- Show:
  - status
  - planned files
  - applied files
  - backup paths
  - diffs
  - warnings
  - rejection reasons

Also show apply health:
- /agent/apply/health

Important:
- Default dry_run should be true.
- User must manually uncheck dry_run and check confirm to apply.
- Do not auto-apply after review.
- Do not run commands after applying.

Do not redesign full UI.
Do not add routing.
Do not remove previous UI sections.

Code quality requirements:

Backend:
- Keep apply logic in apply_changes.py.
- Keep route logic in existing agent.py if it exists.
- Keep planner/coder/reviewer logic unchanged.
- Use pathlib.
- Use clear exceptions.
- Keep functions small.
- Do not log full file contents.
- Write files with UTF-8.
- Normalize line endings reasonably.
- Use atomic write where practical:
  - write temp file
  - replace target

Frontend:
- Keep App.jsx readable.
- Use helper functions.
- Preserve all existing features.
- Handle invalid JSON gracefully.
- Show loading/error states.

Performance requirements:
- Do not process huge files without limits.
- Do not apply more than MAX_REVIEW_FILES files per request.
- No parallel apply jobs.

Success criteria:

1. All previous phase features still work.
2. GET /agent/apply/health works.
3. POST /agent/apply with dry_run=true returns diffs and writes nothing.
4. POST /agent/apply with confirm=false is rejected.
5. POST /agent/apply with unapproved review is rejected.
6. POST /agent/apply with approved review and confirm=true applies changes.
7. Existing files are backed up before modification.
8. New files can be created safely.
9. Delete/rename/command actions are rejected.
10. Files outside WORKSPACE_ROOT are rejected.
11. Frontend can preview/apply changes.
12. No web search or browser control is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 12.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 12 ONLY: Add web search support.

Do not add browser control yet.
Do not add Playwright yet.
Do not auto-ingest full websites.
Do not auto-apply code changes.
Do not run terminal commands automatically.
Do not rewrite frontend except for a simple web search UI.

Your only goal in this phase:
Create a safe web search tool that can search the internet when local RAG knowledge is insufficient, summarize results, and optionally save approved summaries into the local knowledge base later.

IMPORTANT PROTECTION RULES:
- Do not remove any previous features.
- Do not refactor unrelated files.
- If an error is found, touch only the exact file related to that error.
- Do not regenerate the whole project.
- Do not delete existing endpoints.
- Do not change existing API contracts unless absolutely required.
- Web search must not execute downloaded content.
- Web search must not auto-ingest full websites.
- Web search must not modify project files.

Existing features that must continue working:
- All Phase 1 to Phase 11 endpoints and frontend sections.

Preferred search provider:
Use SearXNG if available.

Environment variables:
Add to .env.example:

WEB_SEARCH_PROVIDER=searxng
SEARXNG_BASE_URL=http://host.docker.internal:8080
WEB_SEARCH_MAX_RESULTS=8
WEB_SEARCH_TIMEOUT_SECONDS=20

Backend requirements:

1. Add web_search.py.

Create:

backend/app/web_search.py

Functions:
- search_web(query: str, max_results: int = 8) -> dict
- search_searxng(query: str, max_results: int = 8) -> dict
- normalize_search_results(raw_results) -> list[dict]
- summarize_search_results(query: str, results: list[dict]) -> dict

2. Search behavior.

Endpoint should call SearXNG:

GET {SEARXNG_BASE_URL}/search

Use params:
- q
- format=json
- language=en
- safesearch=1

Return normalized results:

{
  "title": "...",
  "url": "...",
  "snippet": "...",
  "source": "..."
}

3. Add endpoint:

POST /tools/web-search

Request:

{
  "query": "latest FastAPI file upload docs",
  "max_results": 8,
  "summarize": true
}

Response:

{
  "status": "ok",
  "provider": "searxng",
  "query": "...",
  "results": [
    {
      "title": "...",
      "url": "...",
      "snippet": "...",
      "source": "..."
    }
  ],
  "summary": "..."
}

If SearXNG is unavailable:

{
  "status": "error",
  "provider": "searxng",
  "message": "SearXNG is not reachable."
}

Do not crash backend.

4. Add endpoint:

GET /tools/web-search/health

Response if available:

{
  "status": "ok",
  "provider": "searxng",
  "base_url": "..."
}

If unavailable:

{
  "status": "error",
  "provider": "searxng",
  "base_url": "...",
  "message": "..."
}

5. Summarization behavior.

If summarize=true:
- Use CHAT_MODEL or CODER_MODEL through Ollama.
- Give model only titles/snippets/URLs, not full web pages.
- Summary must mention uncertainty.
- Summary must not claim it has read full pages unless it has only snippets.
- Summary should include useful links as URLs.

If summarize=false:
- Return results only.

6. Optional save-later structure.

Do not save web summaries into Qdrant yet.

But response should include:

{
  "can_save_to_memory": true,
  "memory_candidate": {
    "title": "...",
    "content": "...",
    "source_type": "web_search_summary",
    "query": "...",
    "urls": []
  }
}

This will be used in a later phase.

7. Security rules.

Reject or sanitize:
- empty query
- query longer than 500 characters
- max_results above 20
- unsupported provider
- local file URLs
- javascript URLs

Do not:
- execute scripts
- download files
- follow unknown links automatically
- scrape pages yet
- store results automatically

Frontend requirements:

Add simple Web Search section:

- Search query input
- Max results input
- Summarize checkbox
- Search button
- Loading state
- Error display
- Results list:
  - title
  - URL
  - snippet
  - source
- Summary box if present
- Show web search health

Do not redesign full UI.
Do not add routing.
Do not remove previous UI sections.

Docker note:

Do not force SearXNG container into docker-compose unless necessary.
For now, support external SearXNG through SEARXNG_BASE_URL.

If you add optional SearXNG service, keep it commented or clearly optional.

Code quality requirements:

Backend:
- Keep web search logic in web_search.py.
- Add route in a new tools.py router or existing router if clean.
- Use httpx.
- Use timeouts.
- Use clear error handling.
- Do not log full results excessively.
- Do not store search results in DB yet.

Frontend:
- Keep App.jsx readable.
- Use helper functions.
- Preserve all existing features.
- Show loading/error states.

Success criteria:

1. All previous phase features still work.
2. GET /tools/web-search/health works.
3. POST /tools/web-search returns search results if SearXNG is running.
4. If SearXNG is offline, backend returns a clean error.
5. Summarize=true returns a summary using Ollama.
6. Web search does not save to Qdrant automatically.
7. Web search does not control browser.
8. Web search does not apply code changes.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 13.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 13 ONLY: Add safe browser control using Playwright.

Do not add advanced autonomous browsing yet.
Do not add login automation.
Do not submit forms automatically.
Do not enter passwords.
Do not make purchases.
Do not apply code changes automatically.
Do not run terminal commands automatically.
Do not rewrite frontend except for a simple browser-control UI.

Your only goal in this phase:
Create a safe browser tool that can:
1. Open a URL.
2. Read page title.
3. Extract visible page text.
4. Take screenshots.
5. Test local frontend pages.
6. Click/type only when explicitly requested.

IMPORTANT PROTECTION RULES:
- Do not remove any previous features.
- Do not refactor unrelated files.
- If an error is found, touch only the exact file related to that error.
- Do not regenerate the whole project.
- Do not delete existing endpoints.
- Do not change existing API contracts unless absolutely required.
- Browser tool must not execute downloaded files.
- Browser tool must not save passwords.
- Browser tool must not submit forms unless user explicitly confirms.

Existing features that must continue working:
- All Phase 1 to Phase 12 endpoints and frontend sections.

Environment variables:
Add to .env.example:

BROWSER_HEADLESS=true
BROWSER_SCREENSHOT_DIR=/data/screenshots
BROWSER_TIMEOUT_SECONDS=30
BROWSER_ALLOWED_HOSTS=localhost,127.0.0.1,host.docker.internal

Docker requirements:
- Add Playwright dependencies to backend Dockerfile.
- Install Chromium for Playwright inside backend container.
- Mount screenshots directory:
  ./data/screenshots:/data/screenshots

Do not break existing Docker services or volumes.

Backend requirements:

1. Add browser_control.py.

Create:

backend/app/browser_control.py

Functions:
- validate_url(url: str) -> str
- open_page(url: str) -> dict
- get_page_text(url: str) -> dict
- take_screenshot(url: str) -> dict
- click_element(url: str, selector: str, confirm: bool) -> dict
- type_text(url: str, selector: str, text: str, confirm: bool) -> dict

2. Safe URL rules.

Allow by default:
- http://localhost
- http://127.0.0.1
- http://host.docker.internal

Block:
- file:// URLs
- javascript: URLs
- data: URLs
- chrome:// URLs
- about: URLs
- local filesystem paths
- empty URLs

For external websites:
- Allow only read-only actions:
  - open
  - extract text
  - screenshot
- Do not click/type on external sites unless confirm=true and URL is explicitly allowed.

3. Add endpoint:

POST /tools/browser/open

Request:

{
  "url": "http://host.docker.internal:3000"
}

Response:

{
  "status": "ok",
  "url": "...",
  "title": "...",
  "final_url": "..."
}

4. Add endpoint:

POST /tools/browser/text

Request:

{
  "url": "http://host.docker.internal:3000",
  "max_chars": 5000
}

Response:

{
  "status": "ok",
  "url": "...",
  "title": "...",
  "text": "visible page text..."
}

Limit max_chars to 20000.

5. Add endpoint:

POST /tools/browser/screenshot

Request:

{
  "url": "http://host.docker.internal:3000",
  "full_page": true
}

Response:

{
  "status": "ok",
  "url": "...",
  "title": "...",
  "screenshot_path": "/data/screenshots/....png"
}

6. Add endpoint:

POST /tools/browser/click

Request:

{
  "url": "http://host.docker.internal:3000",
  "selector": "button#submit",
  "confirm": true
}

Rules:
- If confirm is not true, reject.
- Selector must not be empty.
- Do not allow click on dangerous external pages without confirmation.
- Return title/final_url after click.

7. Add endpoint:

POST /tools/browser/type

Request:

{
  "url": "http://host.docker.internal:3000",
  "selector": "input[name='search']",
  "text": "hello",
  "confirm": true
}

Rules:
- If confirm is not true, reject.
- Selector must not be empty.
- Text max length 1000.
- Do not type into password fields.
- Do not type sensitive-looking values.
- Return title/final_url after typing.

8. Add endpoint:

GET /tools/browser/health

Response:

{
  "status": "ok",
  "browser": "chromium",
  "headless": true,
  "screenshot_dir": "/data/screenshots"
}

If Playwright is not installed/configured:

{
  "status": "error",
  "message": "..."
}

Do not crash backend.

Frontend requirements:

Add simple Browser Tool section:

- URL input
- Buttons:
  - Open URL
  - Extract Text
  - Take Screenshot
- Optional selector input
- Optional text input
- Confirm checkbox
- Buttons:
  - Click Selector
  - Type Text
- Output area:
  - status
  - title
  - final URL
  - extracted text
  - screenshot path
  - errors

Also show browser health:
- /tools/browser/health

Do not redesign full UI.
Do not add routing.
Do not remove previous UI sections.

Code quality requirements:

Backend:
- Keep browser logic in browser_control.py.
- Add routes to existing tools.py if it exists.
- Use async Playwright if backend is async; otherwise use sync carefully.
- Use clear error handling.
- Close browser/page/context after each operation.
- Do not keep long-running browser sessions yet.
- Do not log full page text.
- Do not expose arbitrary file reads.

Frontend:
- Keep App.jsx readable.
- Use helper functions.
- Preserve all existing features.
- Show loading and error states.

Success criteria:

1. All previous phase features still work.
2. GET /tools/browser/health works.
3. POST /tools/browser/open opens local frontend.
4. POST /tools/browser/text extracts visible text.
5. POST /tools/browser/screenshot creates screenshot file.
6. Click/type endpoints require confirm=true.
7. Password fields are blocked.
8. file:// and javascript: URLs are blocked.
9. No autonomous browsing, login automation, web search modification, or code apply changes are added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 14. done
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 14 ONLY: Clean and organize the frontend UI.

Do not change backend logic.
Do not change API contracts.
Do not add new backend endpoints.
Do not remove existing features.
Do not rewrite the full project.

Your only goal in this phase:
Refactor the frontend into a cleaner dashboard with separate components while preserving every existing feature from Phase 1 to Phase 13.

IMPORTANT PROTECTION RULES:
- Do not remove any frontend feature.
- Do not change backend files unless absolutely necessary for a frontend build error.
- If fixing an error, touch only the exact file related to that error.
- Do not change API endpoint paths.
- Do not rename existing backend response fields.
- Do not add new major libraries unless necessary.
- Do not add authentication yet.
- Do not add routing unless it is simple and does not break anything.

Existing frontend sections that must remain:
1. Health checks
2. PDF upload
3. Documents list
4. Text extraction
5. Chunking
6. Embeddings
7. Qdrant indexing
8. Knowledge search
9. RAG chat
10. Planner agent
11. Coding agent draft changes
12. Reviewer agent
13. Apply changes
14. Web search
15. Browser control

Frontend goal:
Turn the current App.jsx into a cleaner component-based dashboard.

Recommended frontend structure:

frontend/
  src/
    main.jsx
    App.jsx
    api/
      client.js
    components/
      Layout.jsx
      Section.jsx
      StatusBadge.jsx
      JsonViewer.jsx
      LoadingButton.jsx
    features/
      health/
        HealthPanel.jsx
      documents/
        DocumentUpload.jsx
        DocumentList.jsx
        DocumentActions.jsx
      knowledge/
        KnowledgeSearch.jsx
        RagChat.jsx
      agents/
        PlannerPanel.jsx
        CodingAgentPanel.jsx
        ReviewAgentPanel.jsx
        ApplyChangesPanel.jsx
      tools/
        WebSearchPanel.jsx
        BrowserControlPanel.jsx

Requirements:

1. API client.

Create:

frontend/src/api/client.js

It should:
- Use VITE_API_BASE_URL
- Provide helper functions:
  - getJson(path)
  - postJson(path, body)
  - postForm(path, formData)
  - deleteJson(path)
- Handle errors consistently.
- Avoid duplicated fetch logic across components.

2. Shared UI components.

Create simple reusable components:

Section.jsx:
- title
- description optional
- children

StatusBadge.jsx:
- status prop
- show ok/error/loading/unknown styles

LoadingButton.jsx:
- disabled/loading state

JsonViewer.jsx:
- pretty prints JSON safely

Do not install heavy UI libraries.

3. Dashboard layout.

App.jsx should become simple:

- Render title
- Render all feature panels
- Keep state mostly inside each feature panel
- Do not contain hundreds of lines of feature-specific code

4. Preserve all existing behavior.

Every endpoint that worked before must still be callable from the UI.

Do not remove:
- Upload button
- Extract button
- Chunk button
- Embed button
- Index button
- Search
- RAG chat
- Planner
- Coding draft
- Review
- Apply
- Web search
- Browser tools

5. Improve usability.

Add:
- Clear section headings
- Small descriptions
- Loading states
- Error messages
- Success messages
- Refresh buttons where useful
- Basic responsive layout

Use plain CSS or simple inline styles.
If CSS file already exists, use it.
If not, create:

frontend/src/App.css

Do not add Tailwind in this phase unless already present.

6. Guard against frontend crashes.

For JSON textareas:
- Validate JSON before sending.
- Show clear error if invalid.

For optional fields:
- Send null/undefined safely.
- Do not crash if backend returns missing fields.

7. Code quality.

Frontend:
- Keep components small.
- Avoid duplicate fetch code.
- Avoid huge App.jsx.
- Use readable function names.
- Preserve existing environment variables.
- Do not hardcode backend URL except fallback.

Backend:
- Do not modify backend unless a frontend build error requires a tiny compatibility fix.
- If backend must be touched, explain why and modify only that file.

Docker:
- Do not change Docker setup unless frontend build requires it.
- Existing docker compose up --build must still work.

Success criteria:

1. docker compose up --build still works.
2. Frontend opens at http://localhost:3000.
3. Backend docs still open at http://localhost:8000/docs.
4. All previous UI features are still visible.
5. Health checks still work.
6. Documents can still upload/list/extract/chunk/embed/index.
7. Knowledge search still works.
8. RAG chat still works.
9. Planner/coder/reviewer/apply panels still exist.
10. Web search and browser panels still exist.
11. App.jsx is much smaller and delegates to components.
12. No backend feature is removed or renamed.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions

Do not continue to Phase 15.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 15 ONLY: Final polish, optimization, stability, and documentation.

Do not add major new features.
Do not change architecture.
Do not remove existing features.
Do not rename API endpoints.
Do not rewrite the whole project.
Do not refactor unrelated files aggressively.

Your only goal in this phase:
Make the existing project more stable, easier to run, easier to debug, and safer to maintain.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 14.
- If fixing an error, touch only the exact file related to that error.
- Do not remove working code.
- Do not change API contracts unless absolutely necessary.
- Do not delete files.
- Do not add authentication yet.
- Do not add multi-user support yet.
- Do not add cloud deployment yet.

Polish goals:

1. Backend stability

Review backend code and improve only where needed:

- Add consistent error responses.
- Add request validation where missing.
- Add safe timeouts for external calls.
- Make health endpoints reliable.
- Improve startup directory creation:
  - /data/uploads
  - /data/extracted
  - /data/screenshots
  - /workspace/backups
- Make SQLite migrations idempotent.
- Ensure failed document operations do not crash backend.
- Ensure Ollama offline returns clean errors.
- Ensure Qdrant offline returns clean errors.
- Ensure SearXNG offline returns clean errors.
- Ensure Playwright missing returns clean errors.

Do not rewrite all backend modules.
Only patch obvious weaknesses.

2. Logging

Add simple structured logging.

Requirements:
- Use Python logging.
- Do not log full PDF text.
- Do not log full embeddings.
- Do not log full prompts.
- Do not log secrets.
- Log:
  - upload started/completed
  - extraction started/completed/failed
  - chunking started/completed/failed
  - embedding started/completed/failed
  - indexing started/completed/failed
  - chat request success/failure
  - agent request success/failure

3. Frontend polish

Improve frontend only where needed:

- Better loading states.
- Better error messages.
- Better empty states.
- Prevent duplicate button clicks during long operations.
- Make long JSON/code outputs scrollable.
- Keep UI responsive.
- Keep all existing panels visible.
- Do not remove any panel from Phase 14.

4. Docker polish

Check Docker setup.

Improve only if needed:

- Ensure volumes are correct.
- Ensure backend can write to /data.
- Ensure backend can write to /workspace/backups.
- Ensure frontend uses VITE_API_BASE_URL correctly.
- Ensure README commands match docker-compose.yml.
- Do not add unnecessary services.

5. README

Create or update README.md with:

- Project overview
- Hardware recommendation
- Windows setup
- Required external tools:
  - Docker Desktop
  - Ollama on Windows host
- Ollama models to pull:
  - qwen2.5-coder:7b
  - nomic-embed-text
  - qwen3:4b
- How to start:
  docker compose up --build
- URLs:
  - frontend: http://localhost:3000
  - backend docs: http://localhost:8000/docs
  - Qdrant: http://localhost:6333
- Environment variables
- Full workflow:
  1. Upload PDF
  2. Extract text
  3. Chunk
  4. Generate embeddings
  5. Index to Qdrant
  6. Search
  7. RAG chat
  8. Plan task
  9. Draft code
  10. Review code
  11. Dry-run apply
  12. Confirm apply
- Safety notes:
  - RAG does not execute PDF content
  - Browser tool blocks dangerous URLs
  - Apply changes requires review + confirm
  - Backups are created before modification

6. Developer notes

Add a short section in README:

"How to continue development safely"

Include rules:
- Work one phase at a time.
- Do not rewrite unrelated files.
- When fixing a bug, only touch the file causing it.
- Preserve existing API contracts.
- Test after every change.

7. Optional small tests

If simple and safe, add minimal backend tests.

Only add tests if it does not break Docker.

Suggested:
- health endpoint test
- safe path validation test
- PDF hash function test
- JSON extraction helper test

Do not create a complex test framework if project is not ready.

Success criteria:

1. docker compose up --build works.
2. Frontend opens at http://localhost:3000.
3. Backend docs open at http://localhost:8000/docs.
4. All previous features remain visible and usable.
5. Upload/extract/chunk/embed/index workflow still works.
6. Search and RAG chat still work.
7. Planner/coder/reviewer/apply panels still work.
8. Web search returns clean error if SearXNG is offline.
9. Browser health returns clean status.
10. README is accurate.
11. No existing feature is removed.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- any assumptions
- any files intentionally not touched

This is the final polish phase. Do not add Phase 16.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 16 ONLY: Add backup restore and project snapshot tools.

Do not add major new architecture.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.

Goal:
Add a safe way to restore files from backups created by Phase 11 and create manual project snapshots before risky changes.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 15.
- If fixing an error, touch only the exact file related to that error.
- Restore must never run commands.
- Restore must never delete files automatically.
- Restore must require confirm=true.
- Restore must only restore files under WORKSPACE_ROOT.

Backend requirements:

1. Extend existing backup/apply module.

Use existing BACKUP_DIR and WORKSPACE_ROOT.

Add functions:
- list_backups()
- preview_backup_file(backup_id, relative_path)
- restore_backup_file(backup_id, relative_path, confirm)
- create_project_snapshot(name)

2. Add endpoint:

GET /agent/backups

Return backup snapshots:

{
  "status": "ok",
  "backups": [
    {
      "id": "...",
      "created_at": "...",
      "files": ["frontend/src/App.jsx"]
    }
  ]
}

3. Add endpoint:

GET /agent/backups/{backup_id}/preview

Query:
- path=frontend/src/App.jsx

Return:
{
  "status": "ok",
  "backup_id": "...",
  "path": "...",
  "content": "...",
  "current_diff": "..."
}

4. Add endpoint:

POST /agent/backups/{backup_id}/restore

Request:
{
  "path": "frontend/src/App.jsx",
  "confirm": true
}

Rules:
- If confirm=false, reject.
- Restore only requested file.
- Before restoring, create a new backup of current file.
- Return diff.

5. Add endpoint:

POST /agent/snapshots/create

Request:
{
  "name": "before-phase-17"
}

Behavior:
- Copy project files under WORKSPACE_ROOT into BACKUP_DIR/snapshots/{timestamp-name}
- Exclude:
  - node_modules
  - .git
  - dist
  - build
  - __pycache__
  - .venv
  - data
  - backups
  - qdrant storage
- Return snapshot id and file count.

Frontend requirements:

Add Backup/Snapshot section:
- List backups
- Preview selected backup file
- Restore selected file with confirm checkbox
- Create manual snapshot
- Show diff and status

Success criteria:
1. All previous features still work.
2. Backups can be listed.
3. Backup file can be previewed.
4. Restore requires confirm=true.
5. Restore creates backup before overwriting current file.
6. Manual snapshot works.
7. No unrelated files are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 17.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 17 ONLY: Add agent run history and audit logs.

Do not add major new architecture.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.

Goal:
Store a lightweight history of planner, coder, reviewer, apply, web search, and browser tool runs so I can debug what happened later.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 16.
- If fixing an error, touch only the exact file related to that error.
- Do not store full secrets.
- Do not store full embeddings.
- Do not store full PDF text.
- Do not store huge prompts unless truncated.
- Do not automatically replay old actions.
- History is read-only except delete/clear endpoints.

Backend requirements:

1. Add SQLite table:

agent_runs:
- id TEXT PRIMARY KEY
- tool_name TEXT NOT NULL
- status TEXT NOT NULL
- input_summary TEXT
- output_summary TEXT
- error TEXT
- model TEXT
- duration_ms INTEGER
- created_at TEXT NOT NULL

tool_name examples:
- planner
- coder
- reviewer
- apply
- web_search
- browser_open
- browser_text
- browser_screenshot
- rag_chat

2. Add helper module:

backend/app/run_history.py

Functions:
- create_run(tool_name, input_summary, model=None)
- complete_run(run_id, output_summary, status="success")
- fail_run(run_id, error)
- list_runs(limit=50, offset=0, tool_name=None)
- get_run(run_id)
- delete_run(run_id)
- clear_runs(confirm)

3. Integrate minimally.

Add run logging to:
- /agent/plan
- /agent/code
- /agent/review
- /agent/apply
- /chat/rag
- /tools/web-search
- /tools/browser/open
- /tools/browser/text
- /tools/browser/screenshot

Do not rewrite these modules.
Only add small logging calls.

4. Add endpoints:

GET /agent/runs

Query params:
- limit default 50, max 200
- offset default 0
- tool_name optional

GET /agent/runs/{run_id}

DELETE /agent/runs/{run_id}

POST /agent/runs/clear

Request:
{
  "confirm": true
}

If confirm is false, reject.

5. Redaction/truncation rules.

Before storing:
- Truncate input_summary to 2000 chars
- Truncate output_summary to 4000 chars
- Remove obvious secrets:
  - api keys
  - bearer tokens
  - passwords
  - .env values
- Do not store full code files unless already short
- Do not store full browser page text

Frontend requirements:

Add Agent History section:
- List recent runs
- Filter by tool_name
- View run details
- Delete one run
- Clear all runs with confirm checkbox
- Show status, duration, model, created_at

Do not redesign full UI.
Do not remove previous panels.

Success criteria:
1. All previous features still work.
2. Agent/tool calls create history rows.
3. History can be listed and filtered.
4. Secrets are redacted/truncated.
5. Clearing history requires confirm=true.
6. No old action is replayed automatically.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 18. done
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 18 ONLY: Add a safe document ingestion queue.

Do not add major new architecture.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.

Goal:
Add a queue that processes PDFs one at a time through the existing pipeline:

upload
→ extract
→ chunk
→ embed
→ index

This is for my hardware:
- GTX 1650 4GB VRAM
- 32GB RAM
- Ryzen 5 5600H

The queue must avoid parallel heavy jobs.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 17.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite existing extraction/chunking/embedding/indexing logic.
- Reuse existing functions where possible.
- Do not process multiple documents in parallel.
- Do not run model calls concurrently.
- Do not delete documents automatically.
- Do not retry forever.

Backend requirements:

1. Add SQLite table:

ingestion_jobs:
- id TEXT PRIMARY KEY
- document_id TEXT NOT NULL
- status TEXT NOT NULL
- current_step TEXT
- requested_steps TEXT NOT NULL
- error TEXT
- attempts INTEGER DEFAULT 0
- created_at TEXT NOT NULL
- started_at TEXT
- completed_at TEXT

Status values:
- queued
- running
- completed
- failed
- cancelled

current_step values:
- extract
- chunk
- embed
- index

2. Add module:

backend/app/ingestion_queue.py

Functions:
- create_ingestion_job(document_id, requested_steps)
- get_next_queued_job()
- run_ingestion_job(job_id)
- cancel_ingestion_job(job_id)
- list_ingestion_jobs(limit=50, offset=0, status=None)
- get_ingestion_job(job_id)

3. Processing rules:

A job may request steps:

{
  "steps": ["extract", "chunk", "embed", "index"]
}

Rules:
- Run steps in order.
- Skip a step if already successfully completed.
- If a step fails, mark job failed and save error.
- If document is duplicate, mark job failed or skipped with clear message.
- Only one job may run at a time.
- Do not start another job while one is running.
- Store progress in SQLite after each step.
- Do not crash backend if job fails.

4. Add queue runner.

Implement simple safe runner:
- Either background task started by API request, or explicit "run next job" endpoint.
- Prefer explicit endpoint first for safety:
  POST /ingestion/jobs/{job_id}/run

Do not create uncontrolled infinite background loops in this phase.

5. Add endpoints:

POST /ingestion/jobs

Request:
{
  "document_id": "...",
  "steps": ["extract", "chunk", "embed", "index"]
}

Response:
{
  "status": "queued",
  "job_id": "...",
  "document_id": "..."
}

GET /ingestion/jobs

Query:
- limit
- offset
- status optional

GET /ingestion/jobs/{job_id}

POST /ingestion/jobs/{job_id}/run

Behavior:
- If another job is running, reject with clear message.
- Run this job one step at a time.
- Return final job status.

POST /ingestion/jobs/{job_id}/cancel

Behavior:
- Can cancel only queued jobs.
- Do not kill running process in this phase.
- If running, return message: cannot cancel running job safely yet.

6. Add bulk helper endpoint:

POST /ingestion/jobs/create-for-unprocessed

Request:
{
  "steps": ["extract", "chunk", "embed", "index"],
  "limit": 20
}

Behavior:
- Find uploaded documents that are not duplicates.
- Create jobs for documents that are not fully indexed.
- Do not create duplicate queued/running jobs for same document.
- Limit default 20, max 100.

7. Frontend requirements:

Add Ingestion Queue section:
- Create job for selected document
- Create jobs for unprocessed documents
- List jobs
- Filter by status
- Run job button
- Cancel queued job button
- Show:
  - job id
  - document id
  - status
  - current step
  - error
  - created/started/completed time

Do not redesign full UI.
Do not remove previous panels.

Success criteria:
1. All previous features still work.
2. A job can be created for one document.
3. Running job executes extract → chunk → embed → index in order.
4. Already completed steps are skipped.
5. Only one job runs at a time.
6. Failed step marks job failed with error.
7. Bulk job creation does not create duplicates.
8. Frontend can list and run jobs.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 19.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 19 ONLY: Add batch PDF upload.

Do not add major new architecture.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.
Do not automatically ingest after upload unless user explicitly requests it.

Goal:
Allow uploading multiple PDFs at once, while preserving the existing one-PDF-at-a-time processing pipeline.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 18.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite existing single PDF upload logic.
- Reuse existing upload, hash, duplicate detection, and database functions.
- Do not bypass duplicate detection.
- Do not process all PDFs through extraction/chunking/embedding/indexing automatically.
- Uploading multiple PDFs should only save metadata/files.
- Ingestion queue handles processing later.

Backend requirements:

1. Add batch upload endpoint:

POST /documents/upload-batch

Input:
- multipart form files
- field name: files
- accept multiple PDFs

Validation:
- Reject empty file list.
- Reject non-PDF files individually.
- Do not fail whole batch because one file is bad.
- Each file should return its own result.
- Use existing SHA-256 duplicate detection.
- Save only valid non-duplicate PDFs.
- Duplicates should be reported clearly.
- Errors should be reported per file.

Response format:

{
  "status": "completed",
  "total_files": 5,
  "uploaded_count": 3,
  "duplicate_count": 1,
  "failed_count": 1,
  "results": [
    {
      "original_filename": "book1.pdf",
      "status": "uploaded",
      "document_id": "...",
      "file_hash": "...",
      "file_size_bytes": 12345
    },
    {
      "original_filename": "book2.pdf",
      "status": "duplicate",
      "duplicate_of": "...",
      "file_hash": "..."
    },
    {
      "original_filename": "notes.txt",
      "status": "failed",
      "error": "Only PDF files are allowed."
    }
  ]
}

2. Add optional queue integration.

Request may include query param:

create_jobs=true

If create_jobs=true:
- For each successfully uploaded document, create ingestion job.
- Default steps:
  extract, chunk, embed, index
- Do not run jobs automatically.
- Return job_id in result.

Example:

POST /documents/upload-batch?create_jobs=true

Result item:

{
  "original_filename": "book1.pdf",
  "status": "uploaded",
  "document_id": "...",
  "job_id": "..."
}

If ingestion queue from Phase 18 is unavailable, return clean warning and still upload files.

3. Add safety limits.

Use env variables:

BATCH_UPLOAD_MAX_FILES=25
BATCH_UPLOAD_MAX_TOTAL_MB=1000

Update .env.example.

Rules:
- If number of files exceeds limit, reject request.
- If total size exceeds limit, reject request before saving if possible.
- Do not load all PDFs fully into RAM.
- Stream files safely where possible.

4. Frontend requirements.

Add batch upload UI to Documents section or Ingestion Queue section:

- Multiple PDF file input
- Checkbox: create ingestion jobs after upload
- Upload batch button
- Show per-file result table:
  - filename
  - status
  - document id
  - duplicate_of
  - job id
  - error
- Refresh documents button after upload
- Refresh ingestion jobs if jobs were created

Do not remove existing single upload UI.

5. Code quality requirements.

Backend:
- Keep batch upload route in documents.py or a small documents_batch.py router.
- Reuse existing storage/hash helpers.
- Reuse existing database helpers.
- Reuse ingestion queue job creator if create_jobs=true.
- Keep errors per file.
- Do not duplicate large chunks of upload code if helper extraction is cleaner.

Frontend:
- Keep components readable.
- Preserve all existing panels.
- Show loading state.
- Show clear partial success results.

Success criteria:
1. All previous features still work.
2. Single PDF upload still works.
3. Batch upload accepts multiple PDFs.
4. Duplicate PDFs are skipped/reported.
5. Invalid files are reported per file.
6. create_jobs=true creates queued ingestion jobs for uploaded PDFs.
7. Jobs are not auto-run.
8. Frontend shows per-file results.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 20.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 20 ONLY: Add knowledge base collections.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.

Goal:
Allow documents to be grouped into separate knowledge bases/collections, such as:
- programming
- web-dev
- python
- java
- personal-notes
- project-docs

This should help search/RAG use only selected document groups.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 19.
- If fixing an error, touch only the exact file related to that error.
- Do not break existing default Qdrant collection.
- Existing documents should still work even if they are not assigned to a knowledge base.
- Do not re-index all documents automatically.

Backend requirements:

1. Add SQLite table:

knowledge_bases:
- id TEXT PRIMARY KEY
- name TEXT NOT NULL UNIQUE
- description TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

2. Add document mapping table:

knowledge_base_documents:
- id TEXT PRIMARY KEY
- knowledge_base_id TEXT NOT NULL
- document_id TEXT NOT NULL
- created_at TEXT NOT NULL

Unique:
- knowledge_base_id + document_id

3. Default knowledge base.

Create default KB if missing:

name: default
description: Default knowledge base

Existing documents should be treated as part of default unless explicitly assigned elsewhere.

4. Add endpoints:

POST /knowledge-bases

Request:
{
  "name": "programming",
  "description": "Programming books and docs"
}

GET /knowledge-bases

GET /knowledge-bases/{kb_id}

PUT /knowledge-bases/{kb_id}

DELETE /knowledge-bases/{kb_id}

Delete rules:
- Do not delete documents.
- Do not delete Qdrant vectors.
- Only delete KB mapping.
- Do not allow deleting default KB.

5. Add document assignment endpoints:

POST /knowledge-bases/{kb_id}/documents/{document_id}

DELETE /knowledge-bases/{kb_id}/documents/{document_id}

GET /knowledge-bases/{kb_id}/documents

6. Update document list response.

GET /documents should include:

knowledge_bases: [
  {
    "id": "...",
    "name": "programming"
  }
]

7. Update search.

GET /search should support optional query param:

knowledge_base_id=...

Behavior:
- If knowledge_base_id is provided, search only documents assigned to that KB.
- Use Qdrant filter by document_id list.
- If KB has no documents, return empty results.
- If no knowledge_base_id is provided, preserve existing behavior.

8. Update RAG chat.

POST /chat/rag should accept optional:

{
  "message": "...",
  "limit": 6,
  "document_id": null,
  "knowledge_base_id": "..."
}

Behavior:
- If knowledge_base_id is provided, retrieve only from that KB.
- If document_id is provided, it takes priority or combine both safely.
- Preserve existing behavior when no KB is provided.

9. Frontend requirements.

Add Knowledge Bases section:

- Create KB
- List KBs
- Edit KB name/description
- Delete KB except default
- Assign document to KB
- Remove document from KB
- Show document KB tags
- Add KB selector to:
  - Knowledge search
  - RAG chat

Do not remove existing search/chat inputs.

10. Code quality.

Backend:
- Put KB logic in knowledge_bases.py.
- Use database.py helpers where appropriate.
- Keep search/RAG changes minimal.
- Do not duplicate large query logic.
- Use clear errors.

Frontend:
- Add a KnowledgeBasePanel component if frontend is componentized.
- Keep UI simple.
- Preserve all existing panels.

Success criteria:
1. All previous features still work.
2. Can create/list/update/delete KBs.
3. Default KB cannot be deleted.
4. Documents can be assigned/removed.
5. Search can filter by KB.
6. RAG chat can filter by KB.
7. Existing documents still search without KB filter.
8. No documents or vectors are deleted when deleting a KB.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 21.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 21 ONLY: Save approved web search summaries into the local knowledge base.

Do not auto-ingest full websites.
Do not scrape pages automatically.
Do not add browser crawling.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.

Goal:
Allow useful web search summaries from Phase 12 to be saved as knowledge documents, then chunked, embedded, indexed, searched, and used in RAG like uploaded PDFs.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 20.
- If fixing an error, touch only the exact file related to that error.
- Do not save web results automatically.
- User must explicitly approve saving.
- Store summaries, not full websites.
- Do not execute web content.
- Do not download files from web results.

Backend requirements:

1. Add source type support.

Update documents table if needed:

source_type TEXT DEFAULT 'pdf'

Allowed source_type values:
- pdf
- web_summary
- manual_note

Existing uploaded PDFs should remain source_type='pdf'.

2. Add table:

web_memory_sources:
- id TEXT PRIMARY KEY
- document_id TEXT NOT NULL
- query TEXT NOT NULL
- title TEXT
- urls_json TEXT
- summary TEXT NOT NULL
- created_at TEXT NOT NULL

3. Add endpoint:

POST /memory/web-summary

Request:

{
  "query": "latest FastAPI upload docs",
  "title": "FastAPI upload summary",
  "summary": "Useful summarized content...",
  "urls": [
    "https://example.com"
  ],
  "knowledge_base_id": "optional"
}

Validation:
- query required
- summary required
- summary max length 20000 characters
- urls max 20
- only allow http/https URLs
- reject javascript/file/data URLs

Behavior:
- Create a document row with source_type='web_summary'
- Save summary as text file or JSON under /data/web_summaries
- Create extracted-style JSON with one page:
  page_number: 1
  text: summary + sources
- Mark extraction_status='extracted'
- Allow it to be chunked using existing chunk endpoint
- If knowledge_base_id provided, assign document to KB
- Do not auto chunk/embed/index unless request has create_job=true

Optional query param:
create_job=true

If true:
- Create ingestion job for steps:
  chunk, embed, index
- Do not run job automatically

Response:

{
  "status": "saved",
  "document_id": "...",
  "source_type": "web_summary",
  "job_id": "optional"
}

4. Update document list UI/data.

GET /documents should show:
- source_type
- original_filename or title

5. Frontend requirements.

In Web Search section:
- After search summary, show button:
  "Save Summary to Knowledge Base"
- Allow selecting knowledge base
- Checkbox: create ingestion job
- Show saved document_id/job_id

In Documents list:
- Show source_type tag:
  PDF / Web Summary / Manual Note

Success criteria:
1. All previous features still work.
2. Web summaries are not saved automatically.
3. Approved summary can be saved as document.
4. Saved summary can be chunked/embed/indexed.
5. Search and RAG can retrieve saved web summary.
6. Optional KB assignment works.
7. No full websites are crawled or downloaded.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 22.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 22 ONLY: Add manual notes as knowledge documents.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not add a rich text editor yet.

Goal:
Allow me to write or paste manual notes into the app and save them into the knowledge base, similar to web summaries.

Examples:
- project rules
- coding standards
- bug fixes
- architecture decisions
- useful prompts
- API notes
- personal documentation

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 21.
- If fixing an error, touch only the exact file related to that error.
- Do not auto-run ingestion unless user explicitly requests create_job=true.
- Do not modify PDF or web-summary behavior.
- Do not execute note content.

Backend requirements:

1. Source type.

Use existing documents.source_type.

Manual notes should use:

source_type='manual_note'

If source_type column does not exist, add it safely.

2. Add table:

manual_notes:
- id TEXT PRIMARY KEY
- document_id TEXT NOT NULL
- title TEXT NOT NULL
- content TEXT NOT NULL
- tags_json TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

3. Add storage directory:

/data/manual_notes

Save note as JSON:

/data/manual_notes/{document_id}.json

Format:

{
  "document_id": "...",
  "title": "...",
  "content": "...",
  "tags": [],
  "created_at": "...",
  "updated_at": "..."
}

Also create extracted-style JSON:

/data/extracted/{document_id}.json

Format:

{
  "document_id": "...",
  "original_filename": "Manual Note - <title>",
  "page_count": 1,
  "pages": [
    {
      "page_number": 1,
      "text": "<title>\n\n<content>\n\nTags: ..."
    }
  ],
  "extracted_at": "..."
}

Set extraction_status='extracted'.

4. Add endpoint:

POST /memory/manual-note

Request:

{
  "title": "My React coding rules",
  "content": "Use small components. Keep API calls in api/client.js.",
  "tags": ["react", "rules"],
  "knowledge_base_id": "optional"
}

Optional query param:

create_job=true

Validation:
- title required, max 200 chars
- content required, max 50000 chars
- tags max 20
- each tag max 50 chars
- knowledge_base_id optional

Behavior:
- Create document row with source_type='manual_note'
- Save manual note JSON
- Save extracted-style JSON
- Insert manual_notes row
- Assign to KB if knowledge_base_id provided
- If create_job=true, create ingestion job with steps:
  chunk, embed, index
- Do not run job automatically

Response:

{
  "status": "saved",
  "document_id": "...",
  "source_type": "manual_note",
  "job_id": "optional"
}

5. Add endpoint:

GET /memory/manual-notes

Return list:

{
  "notes": [
    {
      "id": "...",
      "document_id": "...",
      "title": "...",
      "tags": [],
      "created_at": "...",
      "updated_at": "..."
    }
  ]
}

6. Add endpoint:

GET /memory/manual-notes/{note_id}

Return full note.

7. Add endpoint:

PUT /memory/manual-notes/{note_id}

Request:

{
  "title": "...",
  "content": "...",
  "tags": []
}

Behavior:
- Update manual_notes
- Update stored manual note JSON
- Update extracted-style JSON
- Mark downstream statuses stale:
  - chunking_status='not_started'
  - embedding_status='not_started'
  - qdrant_status='not_started'
- Do not automatically delete old Qdrant vectors unless existing delete/re-index helper is safe
- Return warning that re-chunk/re-index is needed

8. Add endpoint:

DELETE /memory/manual-notes/{note_id}

Behavior:
- Delete manual note row
- Delete related document using existing document delete logic if possible
- Delete extracted JSON and note JSON
- Delete related Qdrant vectors if existing delete document vector logic exists
- Do not affect other documents

9. Update documents list.

GET /documents should show source_type and title/original_filename clearly.

Frontend requirements:

Add Manual Notes section:

- Title input
- Content textarea
- Tags input comma-separated
- Knowledge base selector
- Checkbox: create ingestion job
- Save note button
- List notes
- View/edit note
- Delete note
- Show document_id and job_id after save

Also update Documents list:
- Show source type:
  - PDF
  - Web Summary
  - Manual Note

Do not redesign full UI.
Do not remove existing panels.

Success criteria:
1. All previous features still work.
2. Manual note can be saved.
3. Manual note becomes a document with source_type manual_note.
4. Manual note can be chunked/embed/indexed.
5. Manual note can be searched and used in RAG.
6. create_job=true creates queued job but does not run it.
7. Editing a note marks it as needing reprocessing.
8. Deleting a note affects only that note/document.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 23.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 23 ONLY: Add RAG answer quality controls.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not add new major architecture.

Goal:
Improve RAG chat reliability by adding:
1. source filtering
2. minimum similarity threshold
3. answer modes
4. source-only mode
5. citation consistency checks

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 22.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the whole RAG system.
- Patch existing rag/search/chat logic minimally.
- Do not remove old /chat/rag behavior.
- New fields must be optional and backward compatible.

Backend requirements:

1. Extend POST /chat/rag request.

Keep existing fields working.

Add optional fields:

{
  "message": "string",
  "limit": 6,
  "document_id": "optional",
  "knowledge_base_id": "optional",
  "score_threshold": 0.55,
  "answer_mode": "balanced",
  "source_only": false
}

Allowed answer_mode values:
- concise
- balanced
- detailed
- code-focused

Defaults:
- score_threshold = null
- answer_mode = balanced
- source_only = false

2. Source filtering.

If score_threshold is provided:
- Filter retrieved chunks below threshold.
- If all chunks are filtered out, return insufficient knowledge response.

Response should include:

{
  "filtered_out_count": 3
}

3. Source-only mode.

If source_only=true:
- Do not call LLM.
- Return top retrieved chunks only.
- Useful for checking retrieval quality.

Response:

{
  "answer": "Source-only mode enabled. Returning retrieved chunks without generation.",
  "sources": [...],
  "used_context_count": 6,
  "filtered_out_count": 0,
  "source_only": true
}

4. Answer modes.

Modify RAG prompt depending on answer_mode:

concise:
- short answer
- minimal explanation
- citations required

balanced:
- normal helpful answer
- citations required

detailed:
- deeper explanation
- structured answer
- citations required

code-focused:
- prioritize implementation details
- include code examples only if supported by context
- citations required

5. Citation consistency.

After LLM response:
- Check whether answer mentions at least one source citation when sources were used.
- Expected citation format:
  [filename, pages X-Y]

If no citation is found:
- Add warning:
  "Answer may be missing citations."

Do not regenerate answer in this phase unless already simple.

6. Response additions.

Extend response with optional fields:

{
  "answer": "...",
  "model": "...",
  "sources": [],
  "used_context_count": 6,
  "filtered_out_count": 0,
  "answer_mode": "balanced",
  "source_only": false,
  "warnings": []
}

7. Frontend requirements.

Update RAG Chat panel:

Add:
- score threshold input
- answer mode dropdown
- source-only checkbox
- show filtered_out_count
- show warnings
- source previews remain visible

Do not remove existing RAG chat UI.

8. Success criteria:

1. All previous features still work.
2. Existing /chat/rag requests still work without new fields.
3. score_threshold filters weak chunks.
4. source_only=true returns sources without calling LLM.
5. answer modes change prompt behavior.
6. Missing citation warning appears when needed.
7. Frontend exposes new controls.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 24.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 24 ONLY: Add hybrid search support.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not replace Qdrant.

Goal:
Improve retrieval by combining:
1. vector semantic search
2. keyword search over chunk text
3. optional combined ranking

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 23.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the whole search/RAG system.
- Patch existing search logic minimally.
- Existing GET /search must still work.

Backend requirements:

1. Add keyword search over SQLite chunks.

Search table:
- document_chunks

Keyword search should:
- Search chunk text using LIKE or SQLite FTS if easy.
- Prefer simple LIKE first if FTS would require risky migration.
- Return:
  - chunk_id
  - document_id
  - filename
  - page_start
  - page_end
  - chunk_index
  - text
  - keyword_score

2. Extend GET /search.

Keep old behavior working.

Add optional query param:

search_mode=vector|keyword|hybrid

Default:
search_mode=vector

Other optional params:
- q
- limit
- document_id
- knowledge_base_id
- score_threshold

Behavior:
- vector: existing Qdrant vector search
- keyword: SQLite keyword search only
- hybrid: combine vector + keyword results

3. Hybrid ranking.

For hybrid mode:
- Run vector search.
- Run keyword search.
- Merge results by chunk_id.
- Normalize scores roughly:
  - vector score: keep as provided
  - keyword score: simple count/rank-based score
- Combined score:
  combined_score = 0.7 * vector_score + 0.3 * keyword_score

Return fields:

{
  "score": 0.82,
  "vector_score": 0.80,
  "keyword_score": 0.55,
  "search_mode": "hybrid"
}

4. Filtering.

Hybrid and keyword search must support:
- document_id
- knowledge_base_id if Phase 20 exists
- limit

Do not break existing filters.

5. Extend RAG chat.

POST /chat/rag should accept optional:

{
  "search_mode": "vector|keyword|hybrid"
}

Default:
vector

RAG should use selected search mode.

6. Frontend requirements.

Update:
- Knowledge Search panel
- RAG Chat panel

Add dropdown:
- Vector
- Keyword
- Hybrid

Show result scores:
- vector_score if available
- keyword_score if available
- combined score

7. Success criteria:

1. All previous features still work.
2. GET /search works as before without search_mode.
3. GET /search?search_mode=keyword works.
4. GET /search?search_mode=hybrid works.
5. RAG chat can use hybrid search.
6. Frontend exposes search mode dropdown.
7. No existing vector search behavior is removed.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 25.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 25 ONLY: Add a reusable prompt library.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not change agent behavior automatically.

Goal:
Allow me to save, edit, reuse, and organize prompts for:
- planner agent
- coding agent
- reviewer agent
- RAG chat
- web search
- browser testing
- custom workflows

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 24.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the whole agent system.
- Prompt library should not automatically override existing prompts.
- Saved prompts are templates; user must explicitly select/use them.

Backend requirements:

1. Add SQLite table:

prompt_templates:
- id TEXT PRIMARY KEY
- name TEXT NOT NULL
- description TEXT
- category TEXT NOT NULL
- template TEXT NOT NULL
- variables_json TEXT
- tags_json TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

Allowed categories:
- planner
- coder
- reviewer
- rag
- web_search
- browser
- custom

2. Add module:

backend/app/prompt_library.py

Functions:
- create_prompt_template(...)
- list_prompt_templates(category=None, tag=None, limit=50, offset=0)
- get_prompt_template(template_id)
- update_prompt_template(template_id, ...)
- delete_prompt_template(template_id)
- render_prompt_template(template_id, variables)

3. Add endpoints:

POST /prompts

Request:
{
  "name": "Safe one-file bug fix",
  "description": "Fix only the file causing the error.",
  "category": "coder",
  "template": "Fix this bug: {{error}}. Only edit {{file_path}}.",
  "variables": ["error", "file_path"],
  "tags": ["bugfix", "safe"]
}

GET /prompts

Query params:
- category optional
- tag optional
- limit
- offset

GET /prompts/{template_id}

PUT /prompts/{template_id}

DELETE /prompts/{template_id}

POST /prompts/{template_id}/render

Request:
{
  "variables": {
    "error": "ImportError",
    "file_path": "backend/app/main.py"
  }
}

Response:
{
  "status": "ok",
  "rendered_prompt": "..."
}

4. Template rules.

Use simple variable syntax:

{{variable_name}}

Validation:
- Template required
- Name required
- Category must be allowed
- Variables must be list of strings
- Missing variable during render should return clear error
- Do not execute templates as code
- Do not allow template to access files or environment variables

5. Seed default prompts.

Add optional seed function that creates defaults only if missing:

- Safe bug fix prompt
- One-phase implementation prompt
- Code review prompt
- RAG citation prompt
- Browser local test prompt

Do not overwrite user-edited prompts.

6. Frontend requirements.

Add Prompt Library section:

- Create prompt form
- Category dropdown
- Tags input
- List prompts
- Filter by category/tag
- View/edit/delete prompt
- Render prompt with variables JSON
- Copy rendered prompt

Do not remove previous UI panels.
Do not automatically inject selected prompt into agents yet.
That can be a later phase.

Success criteria:

1. All previous features still work.
2. Can create prompt template.
3. Can list/filter prompts.
4. Can edit/delete prompt.
5. Can render prompt with variables.
6. Missing variables return clear error.
7. Seed prompts do not overwrite existing prompts.
8. Frontend can manage prompt templates.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 26.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 26 ONLY: Allow agents to optionally use saved prompt templates.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not force templates onto existing agents.

Goal:
Let planner, coder, reviewer, RAG chat, web search, and browser tools optionally use prompt templates from Phase 25.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 25.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the whole agent system.
- Existing agent requests without template_id must behave exactly as before.
- Prompt templates are optional.
- If template rendering fails, return clear error and do not call the model.

Backend requirements:

1. Extend these request bodies with optional fields:

Planner:
POST /agent/plan

Add:
{
  "template_id": "optional",
  "template_variables": {}
}

Coder:
POST /agent/code

Add:
{
  "template_id": "optional",
  "template_variables": {}
}

Reviewer:
POST /agent/review

Add:
{
  "template_id": "optional",
  "template_variables": {}
}

RAG:
POST /chat/rag

Add:
{
  "template_id": "optional",
  "template_variables": {}
}

Web search summarization:
POST /tools/web-search

Add:
{
  "template_id": "optional",
  "template_variables": {}
}

2. Behavior rules.

If template_id is not provided:
- Use existing behavior exactly.

If template_id is provided:
- Load prompt template from prompt library.
- Render using template_variables.
- Also include the normal task/question/context safely.
- Do not let template remove safety rules.
- System safety rules must still be added around the template.

Example:
- Template controls style/instructions.
- Backend still adds hard rules:
  - preserve existing features
  - return strict JSON where required
  - cite sources where required
  - do not write files unless apply endpoint

3. Category validation.

Template category must match endpoint:

- /agent/plan → planner
- /agent/code → coder
- /agent/review → reviewer
- /chat/rag → rag
- /tools/web-search → web_search

If wrong category:
Return 400 with clear error.

4. Prompt rendering.

Use existing prompt_library.render_prompt_template.

Rules:
- Missing variables return error.
- Do not execute template content.
- Do not access env/files from template.
- Do not log full rendered prompt.

5. Coder/reviewer JSON safety.

Even with templates:
- Coder must still return strict proposed_changes JSON.
- Reviewer must still return strict review JSON.
- Planner must still return strict plan JSON.

Template must not be allowed to override output schema.

6. Frontend requirements.

Update these panels:
- Planner
- Coding Agent
- Reviewer Agent
- RAG Chat
- Web Search

Add:
- template selector dropdown filtered by category
- template variables JSON textarea
- clear error if invalid JSON
- send template_id and template_variables only if selected

Do not remove existing fields.

7. Success criteria:

1. All previous features still work.
2. Agent requests without template_id behave as before.
3. Planner can use planner template.
4. Coder can use coder template.
5. Reviewer can use reviewer template.
6. RAG chat can use rag template.
7. Web search summary can use web_search template.
8. Wrong category template is rejected.
9. Missing template variables return clear error.
10. Frontend exposes optional template selectors.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 27.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 27 ONLY: Add a local model manager for Ollama models.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add authentication.
Do not add cloud sync.
Do not add multi-user support.
Do not force model changes automatically.

Goal:
Let me view available Ollama models and choose which model is used for each role:
- planner
- coder
- reviewer
- chat/RAG
- embeddings

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 26.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the whole agent system.
- Existing env defaults must continue working.
- If no custom model is selected, use existing env/default model.
- Do not pull/delete models in this phase.
- Model manager is selection only.

Backend requirements:

1. Add SQLite table:

model_settings:
- id TEXT PRIMARY KEY
- role TEXT NOT NULL UNIQUE
- model_name TEXT NOT NULL
- created_at TEXT NOT NULL
- updated_at TEXT

Allowed roles:
- planner
- coder
- reviewer
- chat
- embedding

2. Add module:

backend/app/model_manager.py

Functions:
- list_ollama_models()
- get_model_setting(role)
- set_model_setting(role, model_name)
- get_effective_model(role)
- validate_model_available(model_name)

3. Effective model behavior.

Use this priority:

1. model_settings table value
2. environment variable
3. hardcoded default

Role defaults:
- planner: PLANNER_MODEL or qwen3:4b
- coder: CODER_MODEL or qwen2.5-coder:7b
- reviewer: REVIEWER_MODEL or qwen2.5-coder:7b
- chat: CHAT_MODEL or qwen2.5-coder:7b
- embedding: EMBEDDING_MODEL or nomic-embed-text

4. Add endpoints:

GET /models/ollama

Response:
{
  "status": "ok",
  "models": [
    {
      "name": "qwen2.5-coder:7b",
      "size": 4680000000,
      "modified_at": "..."
    }
  ]
}

If Ollama offline:
{
  "status": "error",
  "message": "..."
}

GET /models/settings

Response:
{
  "settings": [
    {
      "role": "planner",
      "model_name": "qwen3:4b",
      "source": "database|env|default"
    }
  ]
}

PUT /models/settings/{role}

Request:
{
  "model_name": "qwen2.5-coder:7b"
}

Rules:
- role must be allowed
- model must exist in Ollama unless validate=false query param is used
- return clear error if model missing
- do not pull model automatically

DELETE /models/settings/{role}

Behavior:
- Remove custom setting
- Fall back to env/default

5. Update existing modules to use effective model.

Patch minimally:
- planner uses get_effective_model("planner")
- coder uses get_effective_model("coder")
- reviewer uses get_effective_model("reviewer")
- RAG/chat uses get_effective_model("chat")
- embeddings use get_effective_model("embedding")

Do not rewrite full modules.
Only replace direct env/default model lookup where needed.

6. Frontend requirements.

Add Model Manager section:

- Show Ollama health/models
- Show roles:
  - planner
  - coder
  - reviewer
  - chat
  - embedding
- Dropdown/select model for each role
- Save setting
- Reset setting
- Show source:
  - database
  - env
  - default

Do not add model pulling/deleting yet.
Do not remove old settings fields.

7. Success criteria:

1. All previous features still work.
2. GET /models/ollama lists Ollama models.
3. GET /models/settings shows effective models.
4. PUT /models/settings/{role} changes selected model.
5. DELETE /models/settings/{role} resets to env/default.
6. Planner/coder/reviewer/RAG/embedding use selected model.
7. Missing model gives clear error.
8. No model is pulled or deleted automatically.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 28.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 28 ONLY: Add safe Ollama model pulling.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add model deletion yet.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.
Do not automatically switch models after pulling.

Goal:
Allow me to pull missing Ollama models from the app safely.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 27.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the model manager.
- Do not delete models.
- Do not auto-pull models unless I explicitly request it.
- Do not automatically change role settings after pull.
- Pull one model at a time.

Backend requirements:

1. Extend model_manager.py.

Add functions:
- pull_ollama_model(model_name: str) -> dict
- validate_model_name(model_name: str) -> bool

2. Model name validation.

Allow normal Ollama names like:
- qwen2.5-coder:7b
- qwen3:4b
- nomic-embed-text
- llama3.2:3b

Reject:
- empty names
- names longer than 100 chars
- names with spaces
- shell characters like ; & | > < `
- path traversal
- URLs
- file paths

3. Add endpoint:

POST /models/pull

Request:
{
  "model_name": "qwen3:4b"
}

Behavior:
- Validate model name.
- Check Ollama is reachable.
- Call Ollama pull API.
- Pull only this model.
- Return status.

Use Ollama endpoint:
POST {OLLAMA_BASE_URL}/api/pull

Payload:
{
  "name": "qwen3:4b",
  "stream": false
}

Success response:
{
  "status": "pulled",
  "model_name": "qwen3:4b",
  "message": "Model pulled successfully."
}

Error response:
{
  "status": "error",
  "model_name": "qwen3:4b",
  "message": "..."
}

4. Add endpoint:

GET /models/recommended

Return recommended models for my hardware:

{
  "recommended": [
    {
      "role": "coder",
      "model_name": "qwen2.5-coder:7b",
      "reason": "Best coding balance for 4GB VRAM with quantized local use."
    },
    {
      "role": "planner",
      "model_name": "qwen3:4b",
      "reason": "Smaller model for planning and breaking tasks into phases."
    },
    {
      "role": "embedding",
      "model_name": "nomic-embed-text",
      "reason": "Lightweight local embedding model for RAG."
    }
  ]
}

5. Frontend requirements.

Update Model Manager section:

- Add input: model name to pull
- Add Pull Model button
- Show loading while pulling
- Show success/error
- Add Recommended Models list
- Each recommended model has:
  - model name
  - role
  - reason
  - Pull button

Do not:
- auto-set model role after pulling
- delete models
- hide existing role selector

6. Success criteria:

1. All previous features still work.
2. GET /models/recommended works.
3. POST /models/pull pulls one valid model.
4. Invalid model names are rejected.
5. Ollama offline returns clean error.
6. Pulling does not change role settings automatically.
7. Frontend can pull recommended models.
8. No model deletion is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 29.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 29 ONLY: Add safe Ollama model deletion.

Do not remove existing project features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not delete models automatically.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.

Goal:
Allow me to delete local Ollama models safely from the app, with strict confirmation and protection for models currently assigned to roles.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 28.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the model manager.
- Do not delete any model unless user explicitly requests it.
- Do not delete models currently selected for planner/coder/reviewer/chat/embedding unless force=true.
- Do not delete multiple models at once.
- Deletion must require confirm=true.

Backend requirements:

1. Extend model_manager.py.

Add functions:
- delete_ollama_model(model_name: str, confirm: bool, force: bool = false) -> dict
- get_roles_using_model(model_name: str) -> list[str]

2. Reuse model name validation.

Use existing validation from Phase 28.

Reject:
- empty names
- names longer than 100 chars
- names with spaces
- shell characters
- URLs
- file paths
- path traversal

3. Add endpoint:

POST /models/delete

Request:
{
  "model_name": "qwen3:4b",
  "confirm": true,
  "force": false
}

Behavior:
- Validate model name.
- If confirm is not true, reject.
- Check Ollama is reachable.
- Check if model exists.
- Check if model is currently used by any role through model settings/effective model.
- If used and force=false, reject with roles list.
- If force=true, delete anyway but do not remove role settings automatically.
- Call Ollama delete API.

Use Ollama endpoint:
DELETE {OLLAMA_BASE_URL}/api/delete

Payload:
{
  "name": "qwen3:4b"
}

Success response:
{
  "status": "deleted",
  "model_name": "qwen3:4b",
  "message": "Model deleted successfully."
}

Rejected response:
{
  "status": "rejected",
  "model_name": "qwen3:4b",
  "message": "Model is currently used by roles.",
  "roles_using_model": ["planner"]
}

Error response:
{
  "status": "error",
  "model_name": "qwen3:4b",
  "message": "..."
}

4. Add dry-run endpoint:

POST /models/delete/preview

Request:
{
  "model_name": "qwen3:4b"
}

Response:
{
  "status": "ok",
  "model_name": "qwen3:4b",
  "exists": true,
  "roles_using_model": ["planner"],
  "safe_to_delete": false,
  "reason": "Model is currently assigned to one or more roles."
}

5. Frontend requirements.

Update Model Manager section:

- In model list, add Delete button per model.
- Add preview/delete flow:
  1. User clicks preview delete.
  2. Show model name.
  3. Show roles using model.
  4. Show safety warning.
  5. User must check confirm checkbox.
  6. User may check force checkbox if model is in use.
  7. User clicks Delete Model.

Do not:
- auto-delete
- delete multiple models at once
- hide existing pull/role settings features
- auto-clear role settings after deletion

6. Success criteria:

1. All previous features still work.
2. POST /models/delete/preview shows if model is safe to delete.
3. POST /models/delete requires confirm=true.
4. Deletion is rejected if model is used by role and force=false.
5. force=true allows deletion but does not clear role settings.
6. Invalid model names are rejected.
7. Ollama offline returns clean error.
8. Frontend supports safe delete flow.
9. No other features are removed.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 30.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 30 ONLY: Add settings export/import.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not export private data like PDF text, embeddings, uploaded files, backups, or model files.
Do not add cloud sync.
Do not add authentication.
Do not add multi-user support.

Goal:
Allow me to export and import app configuration so I can back up or move settings.

Export should include:
- model role settings
- knowledge base names/descriptions
- prompt templates
- safe app preferences if any

Export should NOT include:
- uploaded PDFs
- extracted text
- chunks
- embeddings
- Qdrant vectors
- manual note contents
- web summary contents
- backups
- agent run full logs
- Ollama model files
- .env secrets

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 29.
- If fixing an error, touch only the exact file related to that error.
- Do not rewrite the settings/model/prompt/KB systems.
- Import must not overwrite existing settings unless overwrite=true.
- Import must validate schema before writing.
- Import must create a backup of current settings before applying.

Backend requirements:

1. Add module:

backend/app/settings_portability.py

Functions:
- export_settings() -> dict
- validate_import_payload(payload: dict) -> dict
- import_settings(payload: dict, overwrite: bool = false) -> dict
- backup_current_settings() -> dict

2. Export format.

Return JSON:

{
  "app": "local-ai-coding-assistant",
  "version": 1,
  "exported_at": "...",
  "data": {
    "model_settings": [],
    "knowledge_bases": [],
    "prompt_templates": [],
    "preferences": {}
  }
}

3. Add endpoint:

GET /settings/export

Response:
{
  "status": "ok",
  "export": { ... }
}

4. Add endpoint:

POST /settings/import/preview

Request:
{
  "export": { ... }
}

Response:
{
  "status": "ok",
  "valid": true,
  "summary": {
    "model_settings": 5,
    "knowledge_bases": 3,
    "prompt_templates": 10
  },
  "warnings": []
}

5. Add endpoint:

POST /settings/import

Request:
{
  "export": { ... },
  "overwrite": false,
  "confirm": true
}

Rules:
- confirm must be true.
- Validate payload first.
- Create backup before importing.
- If overwrite=false:
  - skip existing records with same unique name/role.
- If overwrite=true:
  - update existing records.
- Do not delete records missing from import.
- Do not import unknown fields blindly.
- Return created/updated/skipped counts.

Response:
{
  "status": "imported",
  "backup_id": "...",
  "created": {},
  "updated": {},
  "skipped": {},
  "warnings": []
}

6. Frontend requirements.

Add Settings Portability section:

- Export settings button
- Show/export JSON
- Download JSON file if easy
- Import JSON textarea/file input
- Preview import button
- Confirm checkbox
- Overwrite checkbox
- Import button
- Show created/updated/skipped/warnings

Do not remove previous panels.

Success criteria:

1. All previous features still work.
2. Settings export works.
3. Export does not include private documents/chunks/vectors.
4. Import preview validates payload.
5. Import requires confirm=true.
6. Import creates backup before changes.
7. overwrite=false skips conflicts.
8. overwrite=true updates conflicts.
9. Frontend supports export/import flow.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 31.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 31 ONLY: Add optional local API key protection for backend endpoints.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add full user accounts.
Do not add cloud login.
Do not add OAuth.
Do not add multi-user support.

Goal:
Protect the local backend with an optional API key so random apps/pages cannot call my local AI assistant endpoints.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 30.
- If fixing an error, touch only the exact file related to that error.
- Authentication must be optional.
- If API key is not configured, app should work exactly as before.
- Do not protect /health by default.
- Do not log API keys.
- Do not export API keys in settings export.

Backend requirements:

1. Environment variables.

Update .env.example:

ENABLE_API_KEY_AUTH=false
LOCAL_API_KEY=

Behavior:
- If ENABLE_API_KEY_AUTH=false, no auth required.
- If ENABLE_API_KEY_AUTH=true, require API key.
- If enabled but LOCAL_API_KEY is empty, backend should start but return clear auth configuration error for protected endpoints.

2. Add module:

backend/app/auth.py

Functions:
- is_auth_enabled()
- get_configured_api_key()
- verify_api_key(request)
- require_api_key dependency

Accepted API key locations:
- Header: X-API-Key
- Header: Authorization: Bearer <key>

3. Protected endpoints.

Protect all non-health endpoints.

Do not require API key for:
- GET /health
- GET /ollama/health
- GET /qdrant/health

Require API key for:
- document upload/delete/extract/chunk/embed/index
- search
- chat
- agents
- tools
- models
- prompts
- settings import/export
- memory
- ingestion jobs

4. Error responses.

Missing key:

{
  "status": "error",
  "message": "Missing API key."
}

Invalid key:

{
  "status": "error",
  "message": "Invalid API key."
}

Auth enabled but no key configured:

{
  "status": "error",
  "message": "API key auth is enabled but LOCAL_API_KEY is not configured."
}

Use HTTP 401 or 403 appropriately.

5. Frontend requirements.

Add API key support:

- Read VITE_API_KEY from env if provided.
- Allow user to enter API key in Settings panel.
- Store API key in browser localStorage only.
- Add API key to protected requests as X-API-Key.
- Do not display key after saving.
- Add clear button to remove saved key.
- If backend returns auth error, show message.

Update .env.example or frontend env example:

VITE_API_KEY=

6. API client update.

Update frontend/src/api/client.js:
- Attach X-API-Key automatically if available.
- Keep existing requests working when no key is required.
- Do not attach empty key.

7. Settings export/import protection.

Ensure settings export does not include:
- LOCAL_API_KEY
- VITE_API_KEY
- any saved browser key

8. Success criteria:

1. All previous features work when ENABLE_API_KEY_AUTH=false.
2. /health remains public.
3. Protected endpoints require key when ENABLE_API_KEY_AUTH=true.
4. X-API-Key works.
5. Authorization Bearer works.
6. Invalid key is rejected.
7. Frontend can save and use API key.
8. API keys are not logged or exported.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 32.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 32 ONLY: Add local usage analytics dashboard.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add cloud analytics.
Do not send telemetry outside my machine.
Do not add user accounts.
Do not add multi-user support.

Goal:
Show local usage stats so I can understand:
- number of documents uploaded
- number of chunks created
- number of embeddings generated
- number of Qdrant indexed chunks
- RAG chat count
- agent run count
- failed jobs
- storage usage
- most used models
- recent activity

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 31.
- If fixing an error, touch only the exact file related to that error.
- Analytics must be local only.
- Do not collect secrets.
- Do not store full prompts, PDF text, embeddings, or API keys.
- Reuse existing SQLite tables where possible.
- Do not create heavy background analytics jobs.

Backend requirements:

1. Add module:

backend/app/analytics.py

Functions:
- get_overview_stats()
- get_document_stats()
- get_ingestion_stats()
- get_agent_stats()
- get_model_usage_stats()
- get_storage_stats()
- get_recent_activity(limit=20)

2. Add endpoint:

GET /analytics/overview

Response:

{
  "status": "ok",
  "documents": {
    "total": 0,
    "pdf": 0,
    "web_summary": 0,
    "manual_note": 0,
    "duplicates": 0
  },
  "chunks": {
    "total": 0,
    "embedded": 0,
    "indexed": 0
  },
  "ingestion": {
    "queued": 0,
    "running": 0,
    "completed": 0,
    "failed": 0
  },
  "agents": {
    "total_runs": 0,
    "failed_runs": 0
  },
  "models": {
    "most_used": []
  },
  "storage": {
    "uploads_mb": 0,
    "extracted_mb": 0,
    "screenshots_mb": 0,
    "backups_mb": 0,
    "total_mb": 0
  }
}

3. Add endpoint:

GET /analytics/recent-activity

Query:
- limit default 20, max 100

Response:

{
  "status": "ok",
  "activity": [
    {
      "type": "document_uploaded|ingestion_job|agent_run|model_change",
      "title": "...",
      "status": "...",
      "created_at": "..."
    }
  ]
}

4. Data sources.

Use existing tables:
- documents
- document_chunks
- chunk_embeddings
- qdrant_index
- ingestion_jobs
- agent_runs
- model_settings

Use filesystem sizes for:
- /data/uploads
- /data/extracted
- /data/screenshots
- /workspace/backups

If a table does not exist, return zero instead of crashing.

5. Frontend requirements.

Add Analytics section:

- Overview cards:
  - Documents
  - Chunks
  - Embeddings
  - Indexed chunks
  - Ingestion jobs
  - Agent runs
  - Storage usage
- Recent activity list
- Refresh button

Do not add charts unless simple.
Do not install chart libraries.
Do not remove existing panels.

6. Success criteria:

1. All previous features still work.
2. GET /analytics/overview works even if some tables are missing.
3. GET /analytics/recent-activity works.
4. Analytics are local only.
5. No secrets or full document text are collected.
6. Frontend displays analytics panel.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 33.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 33 ONLY: Add project rules and guardrails.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add cloud sync.
Do not add multi-user support.
Do not add full permissions system.

Goal:
Add a local rules system that agents must read before planning, coding, reviewing, or applying changes.

Examples of rules:
- Never remove existing features.
- Touch only files related to the current task.
- Do not edit Docker files unless task requires it.
- Prefer small components.
- Use FastAPI backend patterns.
- Use existing API client in frontend.
- Do not create new libraries without reason.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 32.
- If fixing an error, touch only the exact file related to that error.
- Rules must be optional but enabled by default.
- Rules must be local only.
- Do not send rules anywhere except local model prompts.
- Do not execute rule content as code.

Backend requirements:

1. Add SQLite table:

project_rules:
- id TEXT PRIMARY KEY
- title TEXT NOT NULL
- content TEXT NOT NULL
- category TEXT NOT NULL
- enabled INTEGER NOT NULL DEFAULT 1
- priority INTEGER NOT NULL DEFAULT 100
- created_at TEXT NOT NULL
- updated_at TEXT

Allowed categories:
- general
- backend
- frontend
- rag
- agents
- safety
- docker
- style

2. Add module:

backend/app/project_rules.py

Functions:
- create_rule(...)
- list_rules(category=None, enabled=None)
- get_rule(rule_id)
- update_rule(rule_id, ...)
- delete_rule(rule_id)
- get_enabled_rules_text(categories=None)
- seed_default_rules_if_missing()

3. Seed default rules.

Create defaults only if missing:

- Preserve existing features.
- Modify only task-related files.
- Do not rewrite unrelated modules.
- Do not delete files unless explicitly requested.
- Use existing project structure.
- If an error is isolated to one file, patch only that file.
- Keep code modular and readable.
- Do not log secrets.
- Do not execute document contents.

Do not overwrite user-edited rules.

4. Add endpoints:

POST /rules

GET /rules

Query params:
- category optional
- enabled optional

GET /rules/{rule_id}

PUT /rules/{rule_id}

DELETE /rules/{rule_id}

POST /rules/seed-defaults

GET /rules/enabled-text

Query:
- categories optional comma-separated

5. Integrate with agents.

Patch minimally:

- Planner should include enabled categories:
  - general
  - agents
  - safety

- Coder should include enabled categories:
  - general
  - backend/frontend depending on file paths if possible
  - style
  - safety
  - agents

- Reviewer should include enabled categories:
  - general
  - safety
  - agents
  - style

- Apply endpoint should use rules only for warnings if helpful.
  Do not let rules override hard safety checks.

Important:
- Existing behavior without custom rules should remain similar.
- Rules must not override required JSON schemas.
- Rules must not override safety constraints.
- If rules are too long, truncate safely and add warning.

6. Frontend requirements.

Add Project Rules section:

- List rules
- Create rule
- Edit rule
- Enable/disable rule
- Delete rule
- Filter by category
- Seed default rules button
- Show enabled rules text preview

Do not remove existing panels.

7. Success criteria:

1. All previous features still work.
2. Default rules can be seeded.
3. Rules can be created/listed/edited/deleted.
4. Enabled rules are included in planner/coder/reviewer prompts.
5. Disabled rules are ignored.
6. Rules do not break JSON output schemas.
7. Frontend can manage rules.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 34.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 34 ONLY: Add safe full workspace snapshot restore.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add cloud sync.
Do not add multi-user support.

Goal:
Phase 16 added project snapshots. Now add a safe way to restore a full snapshot, but with strict preview, confirmation, exclusions, and backup-before-restore behavior.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 33.
- If fixing an error, touch only the exact file related to that error.
- Full restore must require confirm=true.
- Full restore must require dry_run=false explicitly.
- Full restore must create a new snapshot of current workspace before restoring.
- Full restore must never restore excluded folders.
- Full restore must never delete files by default.
- Do not run terminal commands after restore.

Backend requirements:

1. Extend snapshot/backup module from Phase 16.

Add functions:
- preview_snapshot_restore(snapshot_id)
- restore_workspace_snapshot(snapshot_id, confirm, dry_run, delete_extra_files=false)
- create_pre_restore_snapshot(snapshot_id)

2. Exclusions.

Never restore or delete these:

- node_modules
- .git
- dist
- build
- __pycache__
- .venv
- data
- backups
- qdrant storage
- .env

3. Preview endpoint.

Add:

POST /agent/snapshots/{snapshot_id}/restore-preview

Response:

{
  "status": "ok",
  "snapshot_id": "...",
  "files_to_create": [],
  "files_to_modify": [],
  "files_unchanged": [],
  "excluded_files": [],
  "warnings": []
}

4. Restore endpoint.

Add:

POST /agent/snapshots/{snapshot_id}/restore

Request:

{
  "confirm": true,
  "dry_run": false,
  "delete_extra_files": false
}

Rules:
- If confirm is not true, reject.
- If dry_run is true, do not write anything; return preview.
- Before restore, create current workspace snapshot.
- Restore creates/modifies files from snapshot.
- Do not delete extra files unless delete_extra_files=true.
- Even if delete_extra_files=true, never delete excluded folders/files.
- Return detailed result.

Response:

{
  "status": "restored",
  "snapshot_id": "...",
  "pre_restore_snapshot_id": "...",
  "created": [],
  "modified": [],
  "deleted": [],
  "excluded": [],
  "warnings": []
}

5. Frontend requirements.

Update Backup/Snapshot section:

- List snapshots
- Restore preview button
- Show files to create/modify/delete/excluded
- Confirm checkbox
- Dry run checkbox default true
- Delete extra files checkbox default false
- Restore button
- Show pre-restore snapshot id

6. Success criteria:

1. All previous features still work.
2. Restore preview shows planned changes.
3. Restore requires confirm=true.
4. Dry run writes nothing.
5. Actual restore creates pre-restore snapshot.
6. Excluded folders are never restored/deleted.
7. Extra files are not deleted unless explicitly enabled.
8. No commands are run after restore.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 35.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 35 ONLY: Add local project documentation generator.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add cloud sync.
Do not add multi-user support.
Do not auto-modify source code.

Goal:
Generate local documentation from the current workspace so I can understand the project structure, APIs, agents, tools, and safety flow.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 34.
- If fixing an error, touch only the exact file related to that error.
- Documentation generator must not edit source files.
- Documentation generator must not run terminal commands.
- Documentation generator must not include secrets.
- Documentation generator must not include full uploaded PDFs, chunks, embeddings, or API keys.

Backend requirements:

1. Add module:

backend/app/documentation.py

Functions:
- scan_workspace_files()
- summarize_file_structure()
- extract_backend_routes()
- extract_frontend_components()
- generate_project_docs()
- save_generated_docs(confirm: bool)

2. Add docs output directory:

/workspace/generated_docs

Update .env.example:

GENERATED_DOCS_DIR=/workspace/generated_docs

3. Exclude from scanning:

- node_modules
- .git
- dist
- build
- __pycache__
- .venv
- data
- backups
- qdrant storage
- .env
- uploaded PDFs
- extracted text
- embeddings

4. Add endpoint:

GET /docs-generator/preview

Response:

{
  "status": "ok",
  "summary": {
    "backend_files": 0,
    "frontend_files": 0,
    "routes_found": 0,
    "components_found": 0
  },
  "documentation": {
    "project_overview": "...",
    "folder_structure": "...",
    "backend_routes": [],
    "frontend_components": [],
    "data_flow": "...",
    "safety_model": "..."
  }
}

5. Add endpoint:

POST /docs-generator/save

Request:

{
  "confirm": true
}

Behavior:
- If confirm=false, reject.
- Generate docs.
- Save markdown files to GENERATED_DOCS_DIR:
  - PROJECT_OVERVIEW.md
  - BACKEND_ROUTES.md
  - FRONTEND_COMPONENTS.md
  - RAG_PIPELINE.md
  - AGENT_WORKFLOW.md
  - SAFETY_MODEL.md

Do not overwrite source README unless explicitly requested.
Do not modify app code.

6. Frontend requirements.

Add Documentation Generator section:

- Preview docs button
- Show generated summary
- Save docs checkbox confirm
- Save docs button
- Show generated file paths

Do not remove previous panels.

Success criteria:
1. All previous features still work.
2. Preview generates documentation without writing files.
3. Save requires confirm=true.
4. Generated docs exclude secrets and data.
5. Source code is not modified.
6. Frontend can preview and save docs.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 36.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 36 ONLY: Add a local update/checklist system for project maintenance.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add cloud auto-update.
Do not pull code from GitHub automatically.
Do not install packages automatically.
Do not run terminal commands automatically.

Goal:
Add a safe maintenance checker that reviews the local project status and tells me what may need attention.

It should check:
- Docker config exists
- backend health
- frontend health
- Qdrant health
- Ollama health
- required models available
- data directories exist
- backup directory exists
- generated docs exist
- API key auth config status
- storage usage warning
- failed ingestion jobs
- failed agent runs

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 35.
- If fixing an error, touch only the exact file related to that error.
- Checker must be read-only.
- Checker must not edit files.
- Checker must not run install commands.
- Checker must not pull models.
- Checker must not delete anything.
- Checker must not expose secrets.

Backend requirements:

1. Add module:

backend/app/maintenance.py

Functions:
- check_project_files()
- check_service_health()
- check_required_models()
- check_data_directories()
- check_storage_warnings()
- check_failed_jobs()
- check_failed_agent_runs()
- get_maintenance_report()

2. Add endpoint:

GET /maintenance/report

Response:

{
  "status": "ok",
  "overall_health": "ok|warning|error",
  "checks": [
    {
      "name": "Ollama health",
      "status": "ok|warning|error",
      "message": "...",
      "recommendation": "..."
    }
  ],
  "summary": {
    "ok": 0,
    "warning": 0,
    "error": 0
  }
}

3. Add endpoint:

GET /maintenance/models

Response:

{
  "status": "ok",
  "required_models": [
    {
      "role": "coder",
      "model": "qwen2.5-coder:7b",
      "available": true
    }
  ],
  "missing_models": []
}

4. Add endpoint:

GET /maintenance/storage

Response:

{
  "status": "ok",
  "storage": {
    "uploads_mb": 0,
    "extracted_mb": 0,
    "screenshots_mb": 0,
    "backups_mb": 0,
    "generated_docs_mb": 0,
    "total_mb": 0
  },
  "warnings": []
}

5. Frontend requirements.

Add Maintenance section:

- Run maintenance check button
- Show overall health
- Show checks table:
  - name
  - status
  - message
  - recommendation
- Show required models availability
- Show storage usage
- Refresh button

Do not remove previous panels.

Success criteria:
1. All previous features still work.
2. Maintenance report is read-only.
3. Missing models are reported, not pulled.
4. Storage warnings are shown.
5. Failed jobs/runs are reported.
6. No files are modified by maintenance check.
7. Frontend displays maintenance report.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 37.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 37 ONLY: Add a safe test runner planner.

Do not run tests automatically.
Do not run terminal commands automatically.
Do not install packages.
Do not modify files.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.

Goal:
Add a read-only tool that detects possible test commands and suggests what I should run manually.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 36.
- If fixing an error, touch only the exact file related to that error.
- This phase is planning only.
- Do not execute commands.
- Do not edit package files.
- Do not install dependencies.
- Do not delete files.

Backend requirements:

1. Add module:

backend/app/test_planner.py

Functions:
- scan_test_files()
- detect_backend_test_commands()
- detect_frontend_test_commands()
- detect_docker_test_commands()
- get_test_plan()

2. Exclude scanning:

- node_modules
- .git
- dist
- build
- __pycache__
- .venv
- data
- backups
- qdrant storage
- .env
- uploaded PDFs
- extracted text
- embeddings

3. Detect test files.

Look for:
- backend tests:
  - backend/tests/
  - test_*.py
  - *_test.py
- frontend tests:
  - *.test.jsx
  - *.test.js
  - *.spec.jsx
  - *.spec.js
- config files:
  - pytest.ini
  - package.json
  - vite.config.js

4. Add endpoint:

GET /tests/plan

Response:

{
  "status": "ok",
  "test_files": {
    "backend": [],
    "frontend": []
  },
  "suggested_commands": [
    {
      "name": "Backend pytest",
      "command": "docker compose exec backend pytest",
      "reason": "Detected backend tests."
    }
  ],
  "warnings": []
}

5. Frontend requirements.

Add Test Planner section:

- Run scan button
- Show detected backend test files
- Show detected frontend test files
- Show suggested commands
- Copy command button if easy
- Show warnings

Do not add a run button yet.
Do not execute tests from UI.

Success criteria:
1. All previous features still work.
2. GET /tests/plan works.
3. Test planner detects test files.
4. Suggested commands are shown.
5. No commands are executed.
6. No files are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 38.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 38 ONLY: Add a safe test runner.

Do not install packages.
Do not delete files.
Do not modify source code.
Do not run arbitrary commands.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.

Goal:
Allow the app to run only pre-approved test commands detected/planned in Phase 37.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 37.
- If fixing an error, touch only the exact file related to that error.
- Only run allowlisted test commands.
- Never run user-provided arbitrary shell commands.
- Never run destructive commands.
- Never run install/update commands.
- Require confirm=true before running tests.

Backend requirements:

1. Add module:

backend/app/test_runner.py

Functions:
- get_allowed_test_commands()
- validate_test_command(command_id)
- run_test_command(command_id, confirm)
- store_test_run_result(...)

2. Add SQLite table:

test_runs:
- id TEXT PRIMARY KEY
- command_id TEXT NOT NULL
- command TEXT NOT NULL
- status TEXT NOT NULL
- stdout TEXT
- stderr TEXT
- exit_code INTEGER
- duration_ms INTEGER
- created_at TEXT NOT NULL

3. Allowed commands only.

Allowed command IDs may include:
- backend_pytest
- frontend_tests
- docker_backend_pytest
- docker_frontend_tests

Commands must be internally defined, not accepted raw from user.

Example allowlist:

{
  "backend_pytest": "pytest",
  "docker_backend_pytest": "docker compose exec backend pytest",
  "frontend_tests": "npm test",
  "docker_frontend_tests": "docker compose exec frontend npm test"
}

4. Add endpoint:

GET /tests/allowed

Response:

{
  "status": "ok",
  "commands": [
    {
      "id": "docker_backend_pytest",
      "command": "docker compose exec backend pytest",
      "description": "Run backend tests inside Docker."
    }
  ]
}

5. Add endpoint:

POST /tests/run

Request:

{
  "command_id": "docker_backend_pytest",
  "confirm": true
}

Rules:
- confirm must be true.
- command_id must be allowlisted.
- timeout max 120 seconds.
- capture stdout/stderr.
- store result in SQLite.
- return clean output.
- do not stream yet.

Response:

{
  "status": "completed",
  "run_id": "...",
  "command_id": "...",
  "exit_code": 0,
  "duration_ms": 1234,
  "stdout": "...",
  "stderr": "..."
}

6. Add endpoints:

GET /tests/runs

GET /tests/runs/{run_id}

7. Frontend requirements.

Update Test Planner section or add Test Runner section:

- Show allowed commands
- Confirm checkbox
- Run selected command
- Show stdout/stderr
- Show exit code
- Show recent test runs

Do not allow custom command input.

Success criteria:
1. All previous features still work.
2. GET /tests/allowed works.
3. POST /tests/run rejects confirm=false.
4. POST /tests/run rejects unknown command_id.
5. Allowed test command runs and stores result.
6. Frontend shows test output.
7. No arbitrary commands can run.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 39.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 39 ONLY: Add safe terminal command suggestions.

Do not run terminal commands.
Do not install packages.
Do not delete files.
Do not modify source code.
Do not add arbitrary command execution.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.

Goal:
Allow the app to suggest terminal commands for common tasks, but not execute them.

Examples:
- docker compose up --build
- docker compose logs backend
- docker compose restart backend
- ollama list
- ollama pull qwen2.5-coder:7b
- pytest commands
- npm test commands

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 38.
- If fixing an error, touch only the exact file related to that error.
- This phase is suggestion only.
- Never execute commands.
- Never accept raw user command for execution.
- Never add a run button.
- Suggested commands must be copy-only.
- Dangerous commands must be blocked from suggestions.

Backend requirements:

1. Add module:

backend/app/command_suggestions.py

Functions:
- get_command_categories()
- get_suggested_commands(category=None)
- search_command_suggestions(query)
- validate_suggested_command(command)

2. Add SQLite table:

command_suggestions:
- id TEXT PRIMARY KEY
- title TEXT NOT NULL
- command TEXT NOT NULL
- category TEXT NOT NULL
- description TEXT
- risk_level TEXT NOT NULL
- created_at TEXT NOT NULL

Allowed categories:
- docker
- ollama
- backend
- frontend
- qdrant
- tests
- troubleshooting

Allowed risk_level:
- low
- medium
- high

3. Seed default command suggestions.

Seed only if missing.

Examples:

Docker:
- docker compose up --build
- docker compose down
- docker compose logs backend
- docker compose logs frontend
- docker compose logs qdrant
- docker compose restart backend

Ollama:
- ollama list
- ollama pull qwen2.5-coder:7b
- ollama pull nomic-embed-text
- ollama pull qwen3:4b

Testing:
- docker compose exec backend pytest
- docker compose exec frontend npm test

Troubleshooting:
- docker compose ps
- docker compose logs --tail=100 backend

4. Safety validation.

Do not seed or return commands containing dangerous patterns:

- rm -rf
- del /s
- format
- shutdown
- reboot
- curl | sh
- wget | sh
- powershell -EncodedCommand
- chmod 777
- sudo
- mkfs
- diskpart
- reg delete

If a command contains a dangerous pattern:
- mark rejected
- do not return it as suggestion

5. Add endpoint:

GET /commands/suggestions

Query params:
- category optional
- query optional

Response:

{
  "status": "ok",
  "commands": [
    {
      "id": "...",
      "title": "Start app",
      "command": "docker compose up --build",
      "category": "docker",
      "description": "...",
      "risk_level": "low"
    }
  ]
}

6. Add endpoint:

GET /commands/categories

Response:

{
  "status": "ok",
  "categories": [
    "docker",
    "ollama",
    "backend",
    "frontend",
    "qdrant",
    "tests",
    "troubleshooting"
  ]
}

7. Add endpoint:

POST /commands/seed-defaults

Behavior:
- Seed default commands if missing.
- Do not overwrite existing commands.

8. Frontend requirements.

Add Command Suggestions section:

- Category filter
- Search input
- List commands
- Copy command button
- Show risk level
- Show description
- Seed defaults button

Important:
- No Run button.
- No command execution.
- Copy only.

Success criteria:
1. All previous features still work.
2. Default commands can be seeded.
3. Commands can be listed/filter searched.
4. Dangerous commands are not returned.
5. Frontend shows copy-only commands.
6. No command execution is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 40.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 40 ONLY: Final system audit and consistency check.

Do not add new features.
Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts.
Do not run destructive commands.
Do not install packages unless a missing dependency is clearly required.
Do not refactor the whole project.

Goal:
Audit the full project from Phase 1 to Phase 39 and fix only small broken/inconsistent issues.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 39.
- If fixing an error, touch only the exact file related to that error.
- Do not remove panels, endpoints, tables, or modules.
- Do not rename endpoints.
- Do not regenerate the project.
- Do not simplify by deleting features.
- Do not replace working code with a new architecture.

Audit checklist:

1. Backend routes
Verify all major route groups are still registered:
- health
- documents
- extraction
- chunking
- embeddings
- qdrant/search
- RAG chat
- agents
- tools
- models
- prompts
- rules
- analytics
- maintenance
- tests
- commands
- settings
- memory
- ingestion
- backups/snapshots
- docs generator

2. Frontend panels
Verify all UI sections still exist:
- Health
- Documents
- Knowledge Bases
- Knowledge Search
- RAG Chat
- Planner
- Coding Agent
- Reviewer
- Apply Changes
- Web Search
- Browser Control
- Manual Notes
- Prompt Library
- Model Manager
- Project Rules
- Analytics
- Maintenance
- Test Planner/Runner
- Command Suggestions
- Backups/Snapshots
- Documentation Generator
- Settings Export/Import

3. Environment variables
Check .env.example includes all required variables from previous phases.

4. Docker
Check:
- docker-compose.yml builds
- backend Dockerfile has needed dependencies
- frontend Dockerfile builds
- volumes are correct
- /data paths are mounted
- /workspace paths are mounted

5. Database migrations
Check migrations are idempotent:
- app starts even if tables already exist
- columns are added safely
- missing optional tables return clean errors or zeros

6. Safety checks
Verify:
- apply requires review + confirm
- restore requires confirm
- API key auth is optional
- browser blocks dangerous URLs
- commands suggestions are copy-only
- test runner only runs allowlisted commands
- PDFs are never executed
- secrets are not logged/exported

7. Error handling
Improve only obvious issues:
- Ollama offline
- Qdrant offline
- SearXNG offline
- Playwright missing
- invalid JSON in agent requests
- missing files
- bad document IDs

8. README
Update README if outdated:
- include final feature list
- include setup steps
- include model pulls
- include safety notes
- include troubleshooting commands

Allowed changes:
- small bug fixes
- missing imports
- missing router registration
- missing env vars
- minor frontend display fixes
- README updates
- tiny compatibility patches

Forbidden changes:
- deleting features
- renaming endpoints
- replacing architecture
- rewriting App from scratch
- rewriting backend from scratch
- adding new feature scope
- changing database design unnecessarily

Success criteria:
1. docker compose up --build works.
2. Backend docs open at http://localhost:8000/docs.
3. Frontend opens at http://localhost:3000.
4. All route groups are present.
5. All UI panels are present.
6. README matches current system.
7. No existing feature is removed.
8. Only minimal fixes are made.

Before editing:
List exactly which files you will inspect.
Then list exactly which files you will modify.

After editing:
Return:
- inspected files
- modified files
- exact fixes made
- run commands
- final test checklist
- any remaining known issues

This is an audit phase only. Do not continue to Phase 41.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 41 ONLY: Add full feature debug dashboard and issue report generation.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not run destructive tests.
Do not delete files.
Do not apply fixes automatically.
Do not run arbitrary commands.

Goal:
Create a system that checks every major feature we built, lists issues, and writes a structured issue report file that our agents can later use to fix problems.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 40.
- If fixing an error, touch only the exact file related to that error.
- Debug runner must be mostly read-only.
- Do not upload/delete real user documents during debug unless using safe temporary test files.
- Do not delete models.
- Do not pull models.
- Do not apply code changes.
- Do not restore snapshots.
- Do not execute arbitrary commands.
- Do not expose secrets.

Product features built so far:
1. Docker local app foundation
2. FastAPI backend
3. React frontend
4. Qdrant vector database
5. Ollama model connection
6. PDF upload
7. PDF duplicate detection
8. PDF text extraction
9. Text cleanup and chunking
10. Embedding generation
11. Qdrant indexing
12. Semantic search
13. Basic RAG chat
14. LangGraph planner
15. Coding agent draft changes
16. Reviewer agent
17. Safe apply changes
18. Web search tool
19. Browser control with Playwright
20. Frontend dashboard
21. Backups and snapshots
22. Agent run history
23. Ingestion queue
24. Batch PDF upload
25. Knowledge base collections
26. Save web summaries to knowledge base
27. Manual notes knowledge documents
28. RAG quality controls
29. Hybrid search
30. Prompt library
31. Prompt templates inside agents
32. Model manager
33. Model pulling
34. Model deletion
35. Settings export/import
36. Optional API key protection
37. Local analytics
38. Project rules and guardrails
39. Workspace snapshot restore
40. Documentation generator
41. Maintenance checker
42. Test planner
43. Safe test runner
44. Command suggestions
45. Final system audit

Backend requirements:

1. Add module:

backend/app/feature_debugger.py

Functions:
- get_feature_registry()
- run_feature_debug_checks()
- run_single_feature_check(feature_id)
- write_issue_report(results)
- get_latest_issue_report()
- list_issue_reports()

2. Feature registry.

Create a registry of all major features.

Each feature should include:

{
  "id": "rag_chat",
  "name": "RAG Chat",
  "phase": 7,
  "category": "knowledge",
  "checks": [
    "endpoint_exists",
    "basic_health",
    "safe_request"
  ],
  "destructive": false
}

3. Debug checks.

For each feature, run safe checks only:

Check types:
- endpoint exists in FastAPI routes
- required module imports
- required database table exists
- required directory exists
- required env variable exists or has default
- external dependency health if applicable
- safe API smoke test if non-destructive

Examples:

Health:
- /health
- /ollama/health
- /qdrant/health

Documents:
- check upload endpoint exists
- check documents table exists
- check upload directory exists
- do not upload real file unless using generated tiny temporary PDF

Extraction:
- check extraction module exists
- check extracted directory exists
- check extraction columns exist

Chunking:
- check chunking module exists
- check document_chunks table exists

Embeddings:
- check /embeddings/health
- check embedding model setting
- do not embed large text

Qdrant:
- check /qdrant/collection
- check Qdrant reachable or return warning

Search:
- check /search route exists
- run safe query only if collection exists

RAG:
- check /chat/health
- do not require model response if model missing; report warning

Agents:
- check planner/coder/reviewer/apply endpoints exist
- use dry-run or health checks only
- do not write files

Web search:
- check health
- if SearXNG offline, warning not critical failure

Browser:
- check browser health
- optionally open local frontend only

Models:
- check Ollama model list
- check model settings table

Prompts/rules/settings:
- check tables and endpoints

Tests:
- check test planner and allowed test runner endpoints
- do not run tests unless request has run_safe_tests=true

Commands:
- check suggestions endpoint
- verify no dangerous command suggestions

4. Add endpoint:

GET /debug/features

Response:

{
  "status": "ok",
  "features": [
    {
      "id": "rag_chat",
      "name": "RAG Chat",
      "phase": 7,
      "category": "knowledge"
    }
  ]
}

5. Add endpoint:

POST /debug/run

Request:

{
  "feature_id": "optional",
  "write_report": true,
  "run_safe_tests": false
}

Behavior:
- If feature_id provided, check only that feature.
- Otherwise check all features.
- Do not run destructive checks.
- If write_report=true, save report file.

Response:

{
  "status": "completed",
  "summary": {
    "total": 45,
    "passed": 40,
    "warnings": 3,
    "failed": 2
  },
  "report_path": "/data/debug_reports/...",
  "results": [
    {
      "feature_id": "rag_chat",
      "status": "passed|warning|failed",
      "issues": [
        {
          "severity": "warning|error",
          "title": "...",
          "details": "...",
          "suggested_fix": "...",
          "likely_files": [
            "backend/app/rag.py"
          ]
        }
      ]
    }
  ]
}

6. Issue report file.

Create directory:

/data/debug_reports

Write report as JSON and Markdown.

Files:
- /data/debug_reports/{timestamp}_debug_report.json
- /data/debug_reports/{timestamp}_debug_report.md

Markdown must include:

# Debug Report

## Summary

## Failed Features

## Warnings

## Agent Fix Queue

For each issue:

```text
Issue ID:
Severity:
Feature:
Problem:
Likely files:
Suggested fix:
Safe fix instruction:
Only modify the files listed above unless investigation proves another file is the direct cause.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 42 ONLY: Add pause, resume, and retry controls for ingestion jobs.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not run multiple ingestion jobs in parallel.
Do not add OCR yet.
Do not add reranking yet.
Do not add parent-child retrieval yet.

Goal:
Improve the ingestion queue so large PDF batches can be managed safely.

Add support for:
1. Pause queued/running ingestion flow safely.
2. Resume paused jobs.
3. Retry failed jobs.
4. Retry only failed step.
5. Show better progress per job.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 41.
- If fixing an error, touch only the exact file related to that error.
- Reuse existing ingestion queue logic.
- Do not rewrite extraction/chunking/embedding/indexing modules.
- Do not process multiple documents in parallel.
- Do not delete documents automatically.
- Do not retry forever.

Backend requirements:

1. Update ingestion_jobs table safely.

Add columns if missing:
- paused INTEGER DEFAULT 0
- retry_count INTEGER DEFAULT 0
- max_retries INTEGER DEFAULT 2
- last_failed_step TEXT
- progress_json TEXT
- updated_at TEXT

Status values should now support:
- queued
- running
- paused
- completed
- failed
- cancelled

2. Update ingestion_queue.py.

Add functions:
- pause_ingestion_job(job_id)
- resume_ingestion_job(job_id)
- retry_ingestion_job(job_id, failed_step_only=False)
- update_job_progress(job_id, step, status, details=None)
- get_job_progress(job_id)

3. Pause behavior.

Rules:
- Queued job can become paused.
- Running job should not be killed mid-step.
- If pause is requested while running:
  - set paused=1
  - current step may finish
  - before next step starts, job status becomes paused
- Completed/failed/cancelled jobs cannot be paused.

4. Resume behavior.

Rules:
- Paused job can become queued again.
- It should continue from next incomplete step.
- Already successful steps should be skipped.

5. Retry behavior.

Add endpoint to retry failed jobs.

Rules:
- Only failed jobs can be retried.
- retry_count increases by 1.
- If retry_count exceeds max_retries, reject unless force=true.
- If failed_step_only=true:
  - retry from last_failed_step only if possible.
- Otherwise retry remaining incomplete steps.
- Do not duplicate chunks/embeddings/index entries.
- Reuse existing skip-if-already-completed logic.

6. Add endpoints:

POST /ingestion/jobs/{job_id}/pause

Response:
{
  "status": "paused|pause_requested",
  "job_id": "..."
}

POST /ingestion/jobs/{job_id}/resume

Response:
{
  "status": "queued",
  "job_id": "..."
}

POST /ingestion/jobs/{job_id}/retry

Request:
{
  "failed_step_only": true,
  "force": false
}

Response:
{
  "status": "queued",
  "job_id": "...",
  "retry_count": 1
}

GET /ingestion/jobs/{job_id}/progress

Response:
{
  "job_id": "...",
  "status": "...",
  "current_step": "...",
  "progress": {
    "extract": "completed",
    "chunk": "completed",
    "embed": "failed",
    "index": "not_started"
  },
  "last_failed_step": "embed",
  "error": "..."
}

7. Update existing run behavior.

When running a job:
- Check paused flag before each step.
- Update progress after each step.
- Save last_failed_step on failure.
- Keep old endpoint behavior backward compatible.

8. Frontend requirements.

Update Ingestion Queue section:

Add buttons:
- Pause
- Resume
- Retry
- Retry failed step only

Show:
- progress by step
- retry_count
- last_failed_step
- paused status
- error

Do not remove existing queue controls.

Success criteria:
1. All previous features still work.
2. Queued job can be paused.
3. Paused job can be resumed.
4. Failed job can be retried.
5. Retry failed step only works when possible.
6. Running job respects pause before next step.
7. No duplicate chunks/embeddings/vectors are created.
8. Frontend shows progress and retry controls.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 43.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 43 ONLY: Add OCR detection and optional OCR for scanned PDFs.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not run OCR automatically unless explicitly requested.
Do not replace existing text extraction.
Do not add reranking yet.
Do not add parent-child retrieval yet.

Goal:
Detect PDFs that are likely scanned/image-based and optionally run OCR on them.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 42.
- If fixing an error, touch only the exact file related to that error.
- Existing pypdf text extraction must continue working.
- OCR must be optional because it is heavy.
- OCR should run one PDF at a time.
- OCR should not execute PDF content.
- OCR should not crash backend if OCR tools are missing.

Backend requirements:

1. Add environment variables.

Update .env.example:

OCR_ENABLED=false
OCR_OUTPUT_DIR=/data/ocr
OCR_LANGUAGE=eng
OCR_MIN_TEXT_CHARS_PER_PAGE=50
OCR_SCANNED_PAGE_RATIO_THRESHOLD=0.6

2. Add storage directory:

/data/ocr

Mount it in Docker if needed:

./data/ocr:/data/ocr

3. Add SQLite columns to documents table if missing:

ocr_status TEXT
ocr_error TEXT
ocr_page_count INTEGER
ocr_text_path TEXT
ocr_completed_at TEXT
is_likely_scanned INTEGER DEFAULT 0
extraction_quality_score REAL

Possible ocr_status values:
- not_started
- not_needed
- needed
- running
- completed
- failed
- disabled

4. Add module:

backend/app/ocr.py

Functions:
- analyze_extraction_quality(document_id)
- detect_likely_scanned_pdf(document_id)
- run_ocr_for_document(document_id, force=False)
- merge_ocr_text_with_extracted_pages(document_id)
- check_ocr_dependencies()

5. OCR detection logic.

Use existing extracted JSON from:

/data/extracted/{document_id}.json

Calculate:
- page_count
- empty_pages
- pages_with_low_text
- average_chars_per_page
- scanned_page_ratio
- extraction_quality_score

Rules:
- If many pages have very little text, mark is_likely_scanned=1.
- If extraction quality is good, mark ocr_status=not_needed.
- If likely scanned and OCR_ENABLED=false, mark ocr_status=disabled or needed with clear message.
- Detection should be lightweight and not require OCR tools.

6. Add endpoint:

POST /documents/{document_id}/ocr/analyze

Behavior:
- Requires document exists.
- If not extracted yet, return clear error asking to run extraction first.
- Analyze extracted text quality.
- Update document OCR fields.

Response:

{
  "status": "analyzed",
  "document_id": "...",
  "is_likely_scanned": true,
  "page_count": 100,
  "empty_pages": 85,
  "scanned_page_ratio": 0.85,
  "extraction_quality_score": 0.15,
  "recommendation": "OCR recommended."
}

7. OCR implementation.

Preferred Docker-friendly approach:
Use optional Python libraries if available:
- pdf2image
- pytesseract
- pillow

But do not make the app crash if they are missing.

If dependencies are missing:
Return:

{
  "status": "error",
  "message": "OCR dependencies are not installed/configured."
}

8. Add endpoint:

POST /documents/{document_id}/ocr/run

Request:

{
  "force": false,
  "max_pages": null
}

Behavior:
- Requires OCR_ENABLED=true.
- Requires document exists.
- If document is not likely scanned and force=false, return not_needed.
- Run OCR one page at a time.
- Store OCR text as:

/data/ocr/{document_id}.json

Format:

{
  "document_id": "...",
  "language": "eng",
  "pages": [
    {
      "page_number": 1,
      "text": "..."
    }
  ],
  "completed_at": "..."
}

- Update ocr_status, ocr_text_path, ocr_page_count.
- If max_pages is provided, process only first max_pages for testing.

9. Merge OCR into extracted text.

Add endpoint:

POST /documents/{document_id}/ocr/merge

Behavior:
- Requires OCR completed.
- Merge OCR text into existing extracted JSON.
- For pages where extracted text is empty/too short, use OCR text.
- Save updated extracted JSON.
- Mark extraction_status=extracted.
- Mark chunking/embedding/qdrant statuses as not_started because downstream processing must be rerun.

Response:

{
  "status": "merged",
  "document_id": "...",
  "pages_replaced": 85,
  "message": "OCR text merged. Re-chunk and re-index required."
}

10. Add endpoint:

GET /ocr/health

Response if available:

{
  "status": "ok",
  "enabled": true,
  "dependencies": {
    "pytesseract": true,
    "pdf2image": true
  }
}

If disabled/missing:

{
  "status": "warning",
  "enabled": false,
  "message": "OCR is disabled or dependencies are missing."
}

11. Update ingestion queue.

Do not automatically run OCR in existing pipeline.

Optional:
Allow requested step:

ocr_analyze

But do not run ocr_run unless explicitly included.

Supported steps may include:
- extract
- ocr_analyze
- ocr_run
- ocr_merge
- chunk
- embed
- index

Rules:
- OCR steps are optional.
- Existing jobs without OCR should behave as before.

12. Frontend requirements.

Add OCR controls in Documents section:

For each extracted PDF:
- Show extraction quality score
- Show likely scanned status
- Show OCR status
- Button: Analyze OCR Need
- Button: Run OCR
- Button: Merge OCR Text
- max_pages input for testing
- force OCR checkbox
- Show OCR health

Do not remove existing extraction/chunking controls.

Success criteria:
1. All previous features still work.
2. OCR health endpoint works.
3. OCR analysis detects likely scanned PDFs.
4. OCR does not run automatically.
5. OCR run requires OCR_ENABLED=true.
6. Missing OCR dependencies return clean error.
7. OCR merge updates extracted JSON and marks downstream statuses stale.
8. Frontend shows OCR controls.
9. Existing text PDFs still work without OCR.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 44.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 44 ONLY: Add chapter/section-aware chunking.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not replace existing basic chunking.
Do not add parent-child retrieval yet.
Do not add reranking yet.

Goal:
Improve PDF/book chunking by detecting headings, chapters, sections, and subsections so chunks carry better metadata.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 43.
- If fixing an error, touch only the exact file related to that error.
- Existing chunking endpoint must still work.
- Existing chunks must not be deleted automatically.
- Advanced chunking must be optional.
- Do not damage programming code examples during cleanup/chunking.

Backend requirements:

1. Update document_chunks table safely.

Add columns if missing:

- chunking_strategy TEXT DEFAULT 'basic'
- chapter_title TEXT
- section_title TEXT
- subsection_title TEXT
- heading_path TEXT
- parent_section_id TEXT

Do not break existing rows.

2. Add new SQLite table:

document_sections:
- id TEXT PRIMARY KEY
- document_id TEXT NOT NULL
- section_index INTEGER NOT NULL
- level INTEGER NOT NULL
- title TEXT NOT NULL
- heading_path TEXT
- page_start INTEGER
- page_end INTEGER
- text_preview TEXT
- created_at TEXT NOT NULL

3. Add module:

backend/app/section_chunking.py

Functions:
- detect_headings_from_pages(pages)
- build_section_tree(headings, pages)
- split_sections_into_chunks(sections, chunk_size_tokens=900, overlap_tokens=120)
- chunk_document_by_sections(document_id)
- get_document_sections(document_id)

4. Heading detection.

Detect likely headings using simple heuristics:

- Lines starting with:
  - Chapter 1
  - CHAPTER 1
  - Chapter One
  - Section 1.1
  - 1. Introduction
  - 1.1 Variables
  - 1.1.1 Data Types
- Short uppercase lines
- Lines with low punctuation and title-like length
- Markdown headings if present:
  - #
  - ##
  - ###

Avoid false positives:
- Do not treat code lines as headings.
- Do not treat every short line as heading.
- Do not treat page numbers as headings.
- Do not treat repeated headers/footers as headings.

5. Section metadata.

Each chunk should include:
- chapter_title
- section_title
- subsection_title
- heading_path
- parent_section_id
- page_start
- page_end

Example heading_path:

"Chapter 3: Basics > 3.2 Variables > 3.2.1 Type Declarations"

6. Add endpoint:

POST /documents/{document_id}/chunk/sections

Request:

{
  "chunk_size_tokens": 900,
  "overlap_tokens": 120,
  "replace_existing": false
}

Behavior:
- Requires extraction_status='extracted'.
- Uses extracted JSON.
- Detects sections/headings.
- Creates document_sections rows.
- Creates document_chunks rows with chunking_strategy='section'.
- If replace_existing=false:
  - do not delete existing basic chunks.
- If replace_existing=true:
  - delete only old chunks for this document after creating backup metadata.
  - do not delete Qdrant vectors automatically unless existing safe helper exists.
  - return warning that re-indexing is required.

Response:

{
  "status": "chunked",
  "document_id": "...",
  "strategy": "section",
  "sections_found": 24,
  "chunks_created": 180,
  "warnings": []
}

7. Add endpoint:

GET /documents/{document_id}/sections

Response:

{
  "document_id": "...",
  "sections": [
    {
      "id": "...",
      "section_index": 1,
      "level": 1,
      "title": "Chapter 1: Introduction",
      "heading_path": "Chapter 1: Introduction",
      "page_start": 1,
      "page_end": 12,
      "text_preview": "..."
    }
  ]
}

8. Update chunk listing.

GET /documents/{document_id}/chunks should optionally support:

strategy=basic|section|all

Default:
all or existing behavior, whichever is safer.

Return chunk metadata:
- chunking_strategy
- chapter_title
- section_title
- heading_path

9. Frontend requirements.

Add section-aware chunking controls:

- Button: Section Chunk
- Inputs:
  - chunk size
  - overlap
  - replace existing checkbox
- Show:
  - sections found
  - chunks created
  - warnings
- Add button: View Sections
- Show section list:
  - level
  - title
  - page range
  - preview

Do not remove existing basic chunk button.

10. Success criteria:

1. All previous features still work.
2. Basic chunking still works.
3. Section-aware chunking is optional.
4. Sections are detected and stored.
5. Section chunks include heading metadata.
6. Existing chunks are not deleted unless replace_existing=true.
7. Frontend can run section chunking and view sections.
8. No parent-child retrieval or reranking is added yet.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 45.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 45 ONLY: Add parent-child retrieval for better RAG context.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not replace existing vector search.
Do not add reranking yet.
Do not add OCR changes.
Do not modify chunking logic except where needed for parent metadata.

Goal:
Improve RAG retrieval by searching small chunks but sending larger parent sections to the LLM.

Current behavior:
Question → search chunks → send chunks to LLM

New optional behavior:
Question → search chunks → find parent sections → send parent section context to LLM

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 44.
- If fixing an error, touch only the exact file related to that error.
- Parent-child retrieval must be optional.
- Existing RAG/search behavior must continue working.
- Do not delete existing chunks.
- Do not delete Qdrant vectors.
- Do not re-index everything automatically.

Backend requirements:

1. Use existing Phase 44 metadata if available.

Use:
- document_sections table
- document_chunks.parent_section_id
- document_chunks.heading_path
- document_chunks.chapter_title
- document_chunks.section_title

If section metadata is missing:
- Fall back to existing chunk-only retrieval.

2. Add module:

backend/app/parent_retrieval.py

Functions:
- get_parent_section(section_id)
- get_parent_section_text(section_id, max_chars=12000)
- expand_chunks_to_parent_context(chunks, max_parent_sections=4, max_chars_per_parent=12000)
- build_parent_context_for_rag(search_results, max_parent_sections=4)
- dedupe_parent_contexts(contexts)

3. Parent context behavior.

When search finds chunks:
- Group by parent_section_id.
- Fetch parent section text from document_sections or related chunks.
- If parent section text is too long, truncate safely.
- Preserve source metadata:
  - filename
  - page_start
  - page_end
  - heading_path
  - matched_chunk_ids
  - best_score

If parent_section_id is missing:
- Use the original chunk as fallback context.

4. Add endpoint:

POST /retrieval/parent-context

Request:

{
  "query": "explain variables in Python",
  "limit": 8,
  "knowledge_base_id": "optional",
  "document_id": "optional",
  "search_mode": "vector",
  "max_parent_sections": 4
}

Response:

{
  "status": "ok",
  "query": "...",
  "contexts": [
    {
      "type": "parent_section",
      "section_id": "...",
      "document_id": "...",
      "filename": "...",
      "heading_path": "...",
      "page_start": 12,
      "page_end": 18,
      "best_score": 0.82,
      "matched_chunk_ids": ["..."],
      "text": "..."
    }
  ],
  "fallback_chunks_used": 2
}

5. Extend RAG chat.

POST /chat/rag should accept optional:

{
  "retrieval_strategy": "chunk|parent"
}

Default:
chunk

If retrieval_strategy='parent':
- Run normal search.
- Expand results to parent contexts.
- Build prompt using parent contexts.
- Include sources with heading_path when available.

Response should include:

{
  "retrieval_strategy": "parent",
  "parent_context_count": 4
}

Existing requests without retrieval_strategy must behave as before.

6. Prompt update.

When parent retrieval is used, context format should include:

SOURCE:
filename:
pages:
heading:
matched chunks:
text:

Tell model:
- Use parent section context.
- Cite filename, pages, and heading when available.
- Do not invent citations.

7. Frontend requirements.

Update RAG Chat panel:

- Add retrieval strategy dropdown:
  - Chunk retrieval
  - Parent-section retrieval
- Show:
  - parent_context_count
  - heading_path in sources
  - matched_chunk_ids if useful

Add simple Parent Retrieval tester section or add to Knowledge Search:
- query input
- max parent sections
- run parent context retrieval
- display parent contexts

Do not remove existing search/RAG controls.

8. Success criteria:

1. All previous features still work.
2. Existing /chat/rag works without changes.
3. /retrieval/parent-context works.
4. Parent retrieval uses section metadata when available.
5. Falls back to chunks when no parent section exists.
6. RAG chat can use retrieval_strategy='parent'.
7. Frontend exposes parent retrieval option.
8. No reranking is added yet.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 46.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 46 ONLY: Add optional reranking for retrieval results.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not replace existing vector, keyword, hybrid, or parent retrieval.
Do not add codebase indexing yet.
Do not add RAG evaluation yet.

Goal:
Improve retrieval quality by reranking candidate chunks/contexts after initial search.

Current behavior:
query → Qdrant/keyword/hybrid search → top results → RAG

New optional behavior:
query → retrieve more candidates → reranker scores candidates → best results → RAG

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 45.
- If fixing an error, touch only the exact file related to that error.
- Reranking must be optional.
- Existing search and RAG behavior must continue working.
- If reranker model is unavailable, return warning and fall back to original ranking.
- Do not require GPU-heavy reranker by default.

Backend requirements:

1. Environment variables.

Update .env.example:

RERANK_ENABLED=false
RERANK_MODEL=
RERANK_TOP_N=6
RERANK_CANDIDATE_LIMIT=30

2. Add module:

backend/app/reranker.py

Functions:
- is_rerank_enabled()
- rerank_results(query, results, top_n=6)
- score_result_with_llm(query, result)
- fallback_score_results(query, results)
- check_reranker_health()

3. Reranking strategy.

Use lightweight safe default:

If RERANK_MODEL is configured:
- Use Ollama model to score relevance.
- Score each candidate 0 to 1.
- Keep output strict JSON.

If RERANK_MODEL is empty:
- Use fallback local scoring:
  - keyword overlap
  - title/heading match
  - existing vector score
  - page/section metadata if available

Do not fail if no model is configured.

4. Reranker output.

Each reranked result should include:

{
  "rerank_score": 0.91,
  "original_score": 0.78,
  "rerank_reason": "Mentions Python variables and assignment directly."
}

5. Add endpoint:

POST /retrieval/rerank

Request:

{
  "query": "explain variables in Python",
  "results": [
    {
      "chunk_id": "...",
      "text": "...",
      "score": 0.72,
      "filename": "python.pdf"
    }
  ],
  "top_n": 6
}

Response:

{
  "status": "ok",
  "query": "...",
  "reranked_results": [],
  "reranker_used": "fallback|ollama",
  "warnings": []
}

6. Add endpoint:

GET /retrieval/rerank/health

Response:

{
  "status": "ok|warning|error",
  "enabled": false,
  "model": "",
  "mode": "fallback|ollama",
  "message": "..."
}

7. Extend search.

GET /search should support optional:

rerank=true
candidate_limit=30

Behavior:
- First retrieve candidate_limit results.
- Rerank candidates.
- Return final limit results.
- Existing search without rerank=true behaves exactly as before.

8. Extend RAG chat.

POST /chat/rag should accept optional:

{
  "rerank": true,
  "candidate_limit": 30
}

Behavior:
- Retrieve more candidates.
- Rerank.
- Use best chunks/parent contexts.
- Include rerank metadata in sources.

9. Frontend requirements.

Update:
- Knowledge Search panel
- RAG Chat panel
- Parent Retrieval tester if present

Add:
- Rerank checkbox
- Candidate limit input
- Show rerank score
- Show rerank reason
- Show reranker health

10. Success criteria:

1. All previous features still work.
2. Search works unchanged when rerank=false.
3. Rerank health endpoint works.
4. /retrieval/rerank can rerank given results.
5. /search?rerank=true returns reranked results.
6. /chat/rag with rerank=true uses reranked context.
7. If reranker model is unavailable, fallback reranking or clean warning is used.
8. Frontend exposes rerank controls.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 47.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 47 ONLY: Add codebase indexing into the knowledge base.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add RAG evaluation yet.
Do not add conflict-safe patching yet.
Do not run code automatically.

Goal:
Allow the app to index the current project codebase so the coding agent and RAG chat can understand project files.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 46.
- If fixing an error, touch only the exact file related to that error.
- Codebase indexing must be read-only.
- Do not modify source files.
- Do not index secrets.
- Do not index .env files.
- Do not index node_modules, build outputs, cache folders, uploaded PDFs, extracted text, backups, or Qdrant storage.

Backend requirements:

1. Add source type.

Use documents.source_type:

source_type='codebase_file'

2. Add table:

codebase_files:
- id TEXT PRIMARY KEY
- document_id TEXT NOT NULL
- relative_path TEXT NOT NULL UNIQUE
- language TEXT
- file_hash TEXT NOT NULL
- file_size_bytes INTEGER
- indexed_at TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

3. Add module:

backend/app/codebase_indexing.py

Functions:
- scan_codebase_files()
- detect_language(file_path)
- should_index_file(file_path)
- create_codebase_document(file_path)
- update_codebase_document_if_changed(file_path)
- create_extracted_json_for_code_file(document_id, file_path)
- index_codebase_files(limit=None)

4. Allowed file types.

Index:
- .py
- .js
- .jsx
- .ts
- .tsx
- .json
- .md
- .yml
- .yaml
- .css
- .html
- .sql
- .toml
- .txt

Exclude:
- .env
- .env.*
- secrets
- keys
- certificates
- node_modules
- .git
- dist
- build
- __pycache__
- .venv
- data
- backups
- qdrant storage
- uploaded files
- generated docs if too large

5. Add endpoints:

GET /codebase/files

Response:
{
  "status": "ok",
  "files": [
    {
      "relative_path": "backend/app/main.py",
      "language": "python",
      "file_size_bytes": 1234,
      "indexed": true
    }
  ]
}

POST /codebase/index

Request:
{
  "limit": 50,
  "create_jobs": true,
  "knowledge_base_id": "optional"
}

Behavior:
- Scan allowed code files.
- Create/update document rows with source_type='codebase_file'.
- Create extracted JSON with one page containing code text.
- If create_jobs=true, create ingestion jobs:
  chunk, embed, index
- Do not run jobs automatically.
- Skip unchanged files by hash.

Response:
{
  "status": "ok",
  "created": 10,
  "updated": 2,
  "unchanged": 38,
  "jobs_created": 12
}

6. Update RAG/coding agent.

Do not change behavior automatically.

But allow users to select codebase knowledge base in RAG and coding context if already supported.

7. Frontend requirements.

Add Codebase Indexing section:

- Scan files button
- Show allowed files
- Index codebase button
- Limit input
- Create ingestion jobs checkbox
- Knowledge base selector
- Show created/updated/unchanged/jobs_created

Success criteria:
1. All previous features still work.
2. Codebase scan excludes secrets and heavy folders.
3. Codebase files become knowledge documents.
4. Changed files are detected by hash.
5. Ingestion jobs can be created but not auto-run.
6. RAG can later retrieve indexed codebase chunks.
7. Frontend exposes codebase indexing controls.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 48.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 48 ONLY: Add RAG evaluation tests.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not add conflict-safe patching yet.
Do not auto-fix retrieval issues.
Do not modify documents/chunks/vectors automatically.

Goal:
Create saved evaluation questions to test whether RAG retrieval is finding the right sources and giving useful answers.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 47.
- If fixing an error, touch only the exact file related to that error.
- Evaluation must be read-only.
- Do not delete documents.
- Do not re-index automatically.
- Do not change chunking automatically.
- Do not apply code changes.

Backend requirements:

1. Add SQLite table:

rag_eval_sets:
- id TEXT PRIMARY KEY
- name TEXT NOT NULL UNIQUE
- description TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

2. Add SQLite table:

rag_eval_questions:
- id TEXT PRIMARY KEY
- eval_set_id TEXT NOT NULL
- question TEXT NOT NULL
- expected_keywords_json TEXT
- expected_source_patterns_json TEXT
- knowledge_base_id TEXT
- document_id TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

3. Add SQLite table:

rag_eval_runs:
- id TEXT PRIMARY KEY
- eval_set_id TEXT NOT NULL
- status TEXT NOT NULL
- total_questions INTEGER
- passed INTEGER
- warnings INTEGER
- failed INTEGER
- created_at TEXT NOT NULL

4. Add SQLite table:

rag_eval_results:
- id TEXT PRIMARY KEY
- run_id TEXT NOT NULL
- question_id TEXT NOT NULL
- question TEXT NOT NULL
- status TEXT NOT NULL
- answer TEXT
- sources_json TEXT
- score REAL
- issues_json TEXT
- created_at TEXT NOT NULL

5. Add module:

backend/app/rag_eval.py

Functions:
- create_eval_set(...)
- add_eval_question(...)
- run_eval_set(eval_set_id, options)
- evaluate_single_question(question, options)
- score_eval_result(answer, sources, expected_keywords, expected_source_patterns)
- list_eval_runs(...)
- get_eval_run(...)

6. Scoring rules.

For each question:
- Run RAG chat in source_only=false mode.
- Use selected:
  - search_mode
  - retrieval_strategy
  - rerank
  - score_threshold
  - knowledge_base_id/document_id
- Check answer contains expected keywords if provided.
- Check source filename/page/heading matches expected source patterns if provided.
- If no expectations are provided, mark as warning, not pass.
- Score 0 to 1.

Status:
- passed if score >= 0.7
- warning if score >= 0.4
- failed if score < 0.4

7. Add endpoints:

POST /rag-eval/sets

GET /rag-eval/sets

GET /rag-eval/sets/{set_id}

DELETE /rag-eval/sets/{set_id}

POST /rag-eval/sets/{set_id}/questions

GET /rag-eval/sets/{set_id}/questions

PUT /rag-eval/questions/{question_id}

DELETE /rag-eval/questions/{question_id}

POST /rag-eval/sets/{set_id}/run

Request:
{
  "search_mode": "hybrid",
  "retrieval_strategy": "parent",
  "rerank": true,
  "limit": 6,
  "score_threshold": 0.55
}

GET /rag-eval/runs

GET /rag-eval/runs/{run_id}

8. Frontend requirements.

Add RAG Evaluation section:

- Create eval set
- Add questions
- Expected keywords input
- Expected source patterns input
- Select search mode
- Select retrieval strategy
- Rerank checkbox
- Run eval set
- Show summary:
  - passed
  - warnings
  - failed
- Show per-question:
  - answer
  - sources
  - score
  - issues

Success criteria:
1. All previous features still work.
2. Eval sets can be created.
3. Questions can be added.
4. Eval run executes RAG for each question.
5. Results are scored and saved.
6. Frontend shows eval results.
7. Evaluation does not modify documents/chunks/vectors.
8. No auto-fix is added.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 49.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 49 ONLY: Add conflict-safe patch application.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not replace the existing safe apply system.
Do not run terminal commands automatically.
Do not add auto-fix loops.

Goal:
Improve file application safety by supporting unified diff patches instead of only full-file replacement.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 48.
- If fixing an error, touch only the exact file related to that error.
- Patch application must require review + confirm.
- Patch application must create backups before modifying files.
- Patch application must reject conflicts.
- Patch application must never delete files unless explicitly supported later.
- Do not apply patches outside WORKSPACE_ROOT.

Backend requirements:

1. Extend proposed change format.

Existing full-file proposed changes must continue working.

Add optional patch-based format:

{
  "path": "frontend/src/App.jsx",
  "action": "patch",
  "reason": "Add loading state",
  "patch": "--- a/frontend/src/App.jsx\n+++ b/frontend/src/App.jsx\n..."
}

Allowed actions now:
- create
- modify
- patch

Still reject:
- delete
- rename
- move
- command
- chmod

2. Add module:

backend/app/patch_apply.py

Functions:
- validate_unified_diff(patch_text)
- parse_patch_targets(patch_text)
- check_patch_applies_cleanly(target_path, patch_text)
- apply_unified_patch(target_path, patch_text)
- generate_patch_preview(target_path, patch_text)
- reject_conflicting_patch(reason)

3. Patch safety rules.

Reject patch if:
- path traversal exists
- patch targets a different file than proposed change path
- patch includes deletion of entire file
- patch modifies binary files
- patch has no context lines
- patch does not apply cleanly
- patch touches protected files without approved review
- patch contains too many changed lines unless explicitly approved

4. Dry-run behavior.

Update /agent/apply:

If action="patch":
- dry_run=true should verify patch applies cleanly
- return preview diff
- write nothing

If dry_run=false:
- require confirm=true
- require approved review
- create backup
- apply patch
- return applied result

5. Reviewer update.

Update reviewer to understand action="patch".

Reviewer should check:
- patch is smaller than full-file replacement
- patch only changes relevant lines
- patch does not remove unrelated features
- patch target matches task

6. Coder update.

Do not force coder to output patches.

But allow request option:

{
  "prefer_patch": true
}

When prefer_patch=true:
- coder should return action="patch" when modifying existing files
- create action still uses full content for new files

Existing coder behavior must still work.

7. Frontend requirements.

Update:
- Coding Agent panel:
  - add "Prefer patch output" checkbox
- Reviewer panel:
  - support patch changes in JSON
- Apply panel:
  - show patch preview
  - support action="patch"

8. Success criteria:
1. All previous features still work.
2. Full-file create/modify apply still works.
3. Patch dry-run verifies clean application.
4. Patch apply requires review + confirm.
5. Conflicting patch is rejected.
6. Patch outside workspace is rejected.
7. Reviewer understands patch action.
8. Coder can optionally produce patch actions.
9. Frontend supports patch workflow.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 50.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 50 ONLY: Add change timeline and restore points.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not automatically restore anything.
Do not automatically apply fixes.
Do not run terminal commands automatically.
Do not delete backups or snapshots.

Goal:
Connect agent runs, applied file changes, backups, snapshots, test runs, and debug reports into one timeline so rollback and debugging are easy.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 49.
- If fixing an error, touch only the exact file related to that error.
- Timeline must be append-only by default.
- Restore points must not modify files by themselves.
- Restore must still use existing restore/apply systems.
- Do not expose secrets or full file contents in timeline entries.

Backend requirements:

1. Add SQLite table:

change_timeline:
- id TEXT PRIMARY KEY
- event_type TEXT NOT NULL
- title TEXT NOT NULL
- description TEXT
- phase_number INTEGER
- related_agent_run_id TEXT
- related_test_run_id TEXT
- related_debug_report_path TEXT
- related_snapshot_id TEXT
- related_backup_id TEXT
- files_json TEXT
- status TEXT NOT NULL
- metadata_json TEXT
- created_at TEXT NOT NULL

event_type examples:
- phase_started
- phase_completed
- apply_dry_run
- apply_completed
- backup_created
- snapshot_created
- test_run
- debug_report
- restore_point_created
- restore_completed
- issue_detected
- manual_note

status examples:
- info
- success
- warning
- failed

2. Add SQLite table:

restore_points:
- id TEXT PRIMARY KEY
- name TEXT NOT NULL
- description TEXT
- phase_number INTEGER
- snapshot_id TEXT
- debug_report_path TEXT
- test_run_id TEXT
- agent_run_id TEXT
- status TEXT NOT NULL
- created_at TEXT NOT NULL

status examples:
- good
- risky
- broken
- unknown

3. Add module:

backend/app/change_timeline.py

Functions:
- create_timeline_event(...)
- list_timeline_events(limit=100, offset=0, event_type=None, status=None, phase_number=None)
- get_timeline_event(event_id)
- create_restore_point(...)
- list_restore_points(...)
- get_restore_point(restore_point_id)
- mark_restore_point_status(restore_point_id, status)
- build_recovery_summary(restore_point_id)

4. Integrate minimally with existing systems.

Add small logging calls only.

Log timeline events when:
- /agent/apply dry_run happens
- /agent/apply applies changes
- backup is created
- snapshot is created
- debug report is generated
- test run completes
- restore happens
- RAG eval run completes if Phase 48 exists

Do not rewrite those modules.
Only add small calls.

If integration is risky, add manual timeline endpoint first and integrate only obvious places.

5. Add endpoints:

POST /timeline/events

Request:
{
  "event_type": "manual_note",
  "title": "Phase 50 started",
  "description": "Adding change timeline",
  "phase_number": 50,
  "status": "info",
  "files": [],
  "metadata": {}
}

GET /timeline/events

Query:
- limit default 100, max 500
- offset default 0
- event_type optional
- status optional
- phase_number optional

GET /timeline/events/{event_id}

6. Restore point endpoints:

POST /restore-points

Request:
{
  "name": "Before Phase 51",
  "description": "Stable after Phase 50",
  "phase_number": 50,
  "create_snapshot": true,
  "run_debug": false,
  "status": "good"
}

Behavior:
- If create_snapshot=true, use existing snapshot creation logic.
- If run_debug=true, use existing debug runner safely.
- Create restore point row.
- Create timeline event.
- Do not restore anything.

GET /restore-points

GET /restore-points/{restore_point_id}

PUT /restore-points/{restore_point_id}/status

Request:
{
  "status": "good|risky|broken|unknown"
}

GET /restore-points/{restore_point_id}/recovery-summary

Response:
{
  "restore_point_id": "...",
  "name": "...",
  "snapshot_id": "...",
  "debug_report_path": "...",
  "test_run_id": "...",
  "changed_files_since": [],
  "recommended_recovery_steps": [
    "Review debug report",
    "Restore specific files from snapshot if needed"
  ]
}

7. Frontend requirements.

Add Change Timeline section:

- List timeline events
- Filter by:
  - event type
  - status
  - phase number
- Show:
  - timestamp
  - event type
  - title
  - status
  - related files
  - related debug report/test/snapshot ids

Add Restore Points section:

- Create restore point form
  - name
  - description
  - phase number
  - create snapshot checkbox
  - run debug checkbox
  - status
- List restore points
- Mark status good/risky/broken/unknown
- View recovery summary

Do not remove existing backup/snapshot panels.
This feature connects them; it does not replace them.

8. Success criteria:

1. All previous features still work.
2. Timeline events can be created/listed/viewed.
3. Restore points can be created.
4. Restore point can optionally create snapshot.
5. Restore point can optionally run safe debug.
6. Apply/debug/test/snapshot events are logged when safely integrated.
7. Recovery summary shows where to start rollback/debugging.
8. No restore happens automatically.
9. Frontend shows timeline and restore points.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 51.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 51 ONLY: Add Architecture Understanding Engine.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not modify source files automatically.
Do not generate code changes automatically.

Goal:
Build a system that understands how the project is structured so agents can reason about architecture before making changes.

Current issue:
Agents understand files individually.

Desired behavior:

User asks:
"Add authentication"

Agent understands:

Frontend
 ↓
API Routes
 ↓
Services
 ↓
Database
 ↓
Knowledge Base

and identifies all affected components before planning changes.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 50.
- If fixing an error, touch only the exact file related to that error.
- Architecture analysis must be read-only.
- Do not modify project files.
- Do not run code automatically.
- Do not generate diagrams yet.

Backend requirements:

1. Add SQLite table:

architecture_nodes:
- id TEXT PRIMARY KEY
- node_type TEXT NOT NULL
- name TEXT NOT NULL
- file_path TEXT
- metadata_json TEXT
- created_at TEXT NOT NULL

Node types:
- frontend_component
- api_route
- service
- database_table
- model
- utility
- agent
- knowledge_base
- external_dependency

2. Add SQLite table:

architecture_edges:
- id TEXT PRIMARY KEY
- source_node_id TEXT NOT NULL
- target_node_id TEXT NOT NULL
- relationship_type TEXT NOT NULL
- confidence REAL
- created_at TEXT NOT NULL

Relationship examples:
- calls
- imports
- depends_on
- uses
- stores_in
- retrieves_from
- renders
- invokes

3. Add module:

backend/app/architecture.py

Functions:
- scan_project_architecture()
- detect_frontend_components()
- detect_api_routes()
- detect_services()
- detect_database_tables()
- detect_agents()
- build_relationship_graph()
- get_architecture_summary()
- get_file_dependencies(file_path)

4. Detection rules.

Frontend:
- React components
- pages
- layouts

Backend:
- FastAPI routers
- services
- utilities
- models

Database:
- SQLite tables
- Qdrant collections

Agents:
- planner
- coder
- reviewer
- apply

5. Add endpoint:

POST /architecture/scan

Behavior:
- Scan workspace
- Populate architecture_nodes
- Populate architecture_edges
- Return summary

6. Add endpoint:

GET /architecture/summary

Response:

{
  "status": "ok",
  "frontend_components": 0,
  "api_routes": 0,
  "services": 0,
  "database_tables": 0,
  "agents": 0,
  "relationships": 0
}

7. Add endpoint:

GET /architecture/file-impact

Query:

file_path=backend/app/rag.py

Response:

{
  "file": "backend/app/rag.py",
  "depends_on": [],
  "used_by": [],
  "risk_score": 0.75
}

8. Agent integration.

Planner only.

Before generating plans:
- Retrieve architecture summary.
- Retrieve related dependencies.
- Include architecture context.

Do not modify coder/reviewer yet.

9. Frontend requirements.

Add Architecture section:

- Scan architecture button
- Summary cards
- File impact lookup
- Relationship count

Do not add diagrams yet.

Success criteria:

1. All previous features still work.
2. Architecture scan works.
3. Relationships are stored.
4. Planner receives architecture context.
5. File impact lookup works.
6. No source files are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 52.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 52 ONLY: Add Impact Analysis Engine.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not modify source files automatically.
Do not apply code changes automatically.
Do not run terminal commands automatically.

Goal:
Before an agent changes code, analyze which files, APIs, components, database tables, tests, and features may be affected.

This should reduce agent mistakes like:
- editing unrelated files
- breaking existing features
- changing one endpoint without updating frontend usage
- modifying shared utilities without understanding impact

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 51.
- If fixing an error, touch only the exact file related to that error.
- Impact analysis must be read-only.
- Do not modify project files.
- Do not run tests automatically.
- Do not apply fixes automatically.

Backend requirements:

1. Add SQLite table:

impact_reports:
- id TEXT PRIMARY KEY
- task TEXT NOT NULL
- input_files_json TEXT
- impacted_files_json TEXT
- impacted_routes_json TEXT
- impacted_components_json TEXT
- impacted_tables_json TEXT
- impacted_tests_json TEXT
- risk_score REAL
- risk_level TEXT
- recommendations_json TEXT
- created_at TEXT NOT NULL

Risk levels:
- low
- medium
- high

2. Add module:

backend/app/impact_analysis.py

Functions:
- analyze_task_impact(task, file_paths=None)
- find_impacted_files(file_paths)
- find_impacted_routes(file_paths)
- find_impacted_components(file_paths)
- find_impacted_database_tables(file_paths)
- find_related_tests(file_paths)
- calculate_impact_risk(...)
- create_impact_report(...)
- get_impact_report(report_id)

3. Use architecture data.

Use Phase 51 tables:
- architecture_nodes
- architecture_edges

If architecture data is missing:
- Return warning:
  "Architecture scan has not been run yet."
- Still do a basic file-path based analysis.

4. Add endpoint:

POST /impact/analyze

Request:

{
  "task": "Add authentication to document upload",
  "file_paths": [
    "backend/app/documents.py",
    "frontend/src/features/documents/DocumentUpload.jsx"
  ]
}

Response:

{
  "status": "ok",
  "report_id": "...",
  "risk_level": "medium",
  "risk_score": 0.62,
  "impacted_files": [],
  "impacted_routes": [],
  "impacted_components": [],
  "impacted_tables": [],
  "impacted_tests": [],
  "recommendations": [
    "Review upload endpoint and frontend upload panel together.",
    "Run document upload smoke test after changes."
  ],
  "warnings": []
}

5. Add endpoint:

GET /impact/reports

Return recent impact reports.

6. Add endpoint:

GET /impact/reports/{report_id}

Return full report.

7. Integrate with coding agent.

Patch Phase 9 coding agent minimally:

POST /agent/code should accept optional:

{
  "impact_report_id": "optional"
}

If provided:
- Load impact report.
- Include impacted files and recommendations in coder prompt.
- Tell coder:
  - only modify files justified by impact report
  - if extra files are needed, list them as warnings instead of editing them

Existing requests without impact_report_id must behave as before.

8. Integrate with reviewer agent.

Patch Phase 10 reviewer minimally:

POST /agent/review should accept optional:

{
  "impact_report_id": "optional"
}

Reviewer should flag:
- proposed changes outside impacted files
- missing changes to high-risk impacted files
- changes that ignore recommendations

Existing requests without impact_report_id must behave as before.

9. Frontend requirements.

Add Impact Analysis section:

- Task textarea
- File paths textarea
- Analyze button
- Show:
  - risk score
  - risk level
  - impacted files
  - impacted routes
  - impacted components
  - impacted tables
  - related tests
  - recommendations
  - warnings

Update Coding Agent panel:
- optional impact_report_id input/dropdown

Update Reviewer panel:
- optional impact_report_id input/dropdown

Success criteria:

1. All previous features still work.
2. /impact/analyze works.
3. Impact reports are saved.
4. Impact analysis uses architecture graph if available.
5. Coding agent can use impact_report_id.
6. Reviewer can use impact_report_id.
7. No files are modified by impact analysis.
8. Frontend exposes impact analysis.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 53.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 53 ONLY: Add Test Generation Agent.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not apply generated tests automatically.
Do not run tests automatically.
Do not install packages automatically.

Goal:
Generate test files or test patches for changed/impacted code so the system can verify features more safely.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 52.
- If fixing an error, touch only the exact file related to that error.
- Test generation must only draft proposed test changes.
- It must not write files directly.
- It must not run tests directly.
- It must not modify source code.
- Generated tests must go through reviewer + apply workflow.

Backend requirements:

1. Add module:

backend/app/test_generator.py

Functions:
- build_test_generation_prompt(task, file_paths, impact_report_id=None, extra_context=None)
- read_relevant_files_for_tests(file_paths)
- call_test_generator_model(prompt)
- extract_test_changes_json(raw_text)
- generate_test_changes(...)

2. Use model settings.

Use effective model role:
- coder

Fallback:
- qwen2.5-coder:7b

3. Add endpoint:

POST /agent/generate-tests

Request:

{
  "task": "Add tests for document upload duplicate detection",
  "file_paths": [
    "backend/app/documents.py",
    "backend/app/storage.py"
  ],
  "impact_report_id": "optional",
  "extra_context": "Use pytest. Do not change source files."
}

Response:

{
  "status": "ok",
  "model": "qwen2.5-coder:7b",
  "result": {
    "summary": "...",
    "test_strategy": "...",
    "files_read": [],
    "proposed_changes": [
      {
        "path": "backend/tests/test_documents.py",
        "action": "create|modify|patch",
        "reason": "...",
        "content": "..."
      }
    ],
    "commands_to_run_manually": [
      "docker compose exec backend pytest"
    ],
    "warnings": []
  }
}

4. Test generation rules.

The agent must:
- Prefer tests over source changes.
- Create/modify only test files unless explicitly requested.
- Use existing project testing style if detected.
- If no test framework exists, propose minimal setup but do not install packages.
- Include commands to run manually or through the safe test runner.
- Preserve existing tests.
- Avoid snapshot tests unless useful.
- Avoid fragile tests that depend on exact model output.

5. Safety validation.

Reject proposed changes if:
- path is outside WORKSPACE_ROOT
- action is delete/rename/command
- source files are changed without explicit permission
- test file path is suspicious
- content is empty

6. Integration with impact analysis.

If impact_report_id is provided:
- Use impacted files and related tests.
- Prioritize tests for high-risk impacted files.
- Mention untested impacted areas in warnings.

7. Frontend requirements.

Add Test Generation section:

- Task textarea
- File paths textarea
- Optional impact_report_id input/dropdown
- Extra context textarea
- Generate tests button
- Show:
  - summary
  - test strategy
  - proposed changes
  - commands to run
  - warnings

Do not add apply button here.
User should pass proposed test changes to existing reviewer/apply flow.

8. Success criteria:

1. All previous features still work.
2. /agent/generate-tests works.
3. Test agent drafts tests only.
4. Test agent does not write files.
5. Test agent can use impact report.
6. Proposed tests can be reviewed by existing reviewer.
7. Frontend exposes test generation.
8. No tests are run automatically.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 54.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 54 ONLY: Add Coverage Analysis.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not install packages automatically.
Do not run tests automatically.
Do not modify source files.

Goal:
Analyze test coverage reports so the system can see which files/features are tested and which are risky.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 53.
- If fixing an error, touch only the exact file related to that error.
- Coverage analysis must be read-only.
- Do not generate tests in this phase.
- Do not apply fixes.
- Do not run arbitrary commands.

Backend requirements:

1. Add SQLite table:

coverage_reports:
- id TEXT PRIMARY KEY
- source TEXT NOT NULL
- report_path TEXT
- summary_json TEXT
- file_coverage_json TEXT
- uncovered_files_json TEXT
- low_coverage_files_json TEXT
- created_at TEXT NOT NULL

2. Add module:

backend/app/coverage_analysis.py

Functions:
- find_coverage_reports()
- parse_python_coverage_xml(path)
- parse_javascript_coverage_json(path)
- analyze_coverage()
- get_latest_coverage_report()
- get_coverage_for_file(file_path)

3. Supported report formats.

Detect and parse if present:
- coverage.xml
- backend/coverage.xml
- frontend/coverage/coverage-summary.json
- coverage/coverage-summary.json

If no coverage report exists:
Return a clean warning:
"No coverage report found. Run tests with coverage first."

Do not run coverage commands automatically.

4. Add endpoint:

POST /coverage/analyze

Behavior:
- Search for coverage reports.
- Parse available reports.
- Store summary in SQLite.
- Return summary.

Response:

{
  "status": "ok",
  "report_id": "...",
  "summary": {
    "total_files": 0,
    "average_coverage": 0,
    "low_coverage_count": 0
  },
  "low_coverage_files": [],
  "warnings": []
}

5. Add endpoint:

GET /coverage/latest

Return latest report.

6. Add endpoint:

GET /coverage/file

Query:
file_path=backend/app/documents.py

Return:

{
  "status": "ok",
  "file_path": "...",
  "coverage": 72.5,
  "uncovered_lines": [],
  "risk_level": "low|medium|high"
}

7. Integrate with impact analysis.

Patch impact analysis minimally:
- If coverage data exists, include coverage for impacted files.
- Increase risk if impacted file has low/no coverage.
- Add recommendation:
  "Generate tests for low-coverage impacted files."

8. Frontend requirements.

Add Coverage Analysis section:

- Analyze coverage button
- Latest report summary
- Low coverage files table
- File coverage lookup
- Warnings

Update Impact Analysis display:
- Show coverage risk if present

Success criteria:

1. All previous features still work.
2. /coverage/analyze works.
3. Missing coverage report returns clean warning.
4. Coverage report is stored.
5. File coverage lookup works.
6. Impact analysis includes coverage risk when available.
7. No tests are run automatically.
8. No files are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 55.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 55 ONLY: Add Regression Detection.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not install packages automatically.
Do not run tests automatically unless using existing safe test runner with explicit confirm.
Do not modify source files.
Do not apply fixes automatically.

Goal:
Detect when a previously working feature becomes broken after changes.

Regression detection should compare:
- old debug reports
- new debug reports
- old test runs
- new test runs
- restore points
- timeline events

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 54.
- If fixing an error, touch only the exact file related to that error.
- Regression detection must be read-only.
- Do not restore automatically.
- Do not apply fixes automatically.
- Do not delete reports.
- Do not hide failures.

Backend requirements:

1. Add SQLite table:

regression_reports:
- id TEXT PRIMARY KEY
- baseline_debug_report_path TEXT
- current_debug_report_path TEXT
- baseline_test_run_id TEXT
- current_test_run_id TEXT
- restore_point_id TEXT
- summary_json TEXT
- regressions_json TEXT
- improvements_json TEXT
- unchanged_failures_json TEXT
- created_at TEXT NOT NULL

2. Add module:

backend/app/regression_detection.py

Functions:
- load_debug_report(path)
- load_test_run(run_id)
- compare_debug_reports(baseline_path, current_path)
- compare_test_runs(baseline_run_id, current_run_id)
- detect_regressions(...)
- create_regression_report(...)
- get_regression_report(report_id)
- list_regression_reports(...)

3. Regression logic.

A regression means:
- feature was passed before, failed now
- feature was warning before, failed now
- test passed before, failed now
- endpoint existed before, missing now
- UI panel existed before, missing now
- health check was ok before, error now

An improvement means:
- failed before, passed now
- warning before, passed now

Unchanged failure means:
- failed before and still failed

4. Add endpoint:

POST /regression/detect

Request:

{
  "baseline_debug_report_path": "/data/debug_reports/old_debug_report.json",
  "current_debug_report_path": "/data/debug_reports/new_debug_report.json",
  "baseline_test_run_id": "optional",
  "current_test_run_id": "optional",
  "restore_point_id": "optional"
}

Response:

{
  "status": "ok",
  "report_id": "...",
  "summary": {
    "regressions": 2,
    "improvements": 3,
    "unchanged_failures": 1
  },
  "regressions": [
    {
      "feature_id": "rag_chat",
      "previous_status": "passed",
      "current_status": "failed",
      "likely_files": ["backend/app/rag.py"],
      "suggested_start_point": "Check changes after restore point ..."
    }
  ]
}

5. Add endpoint:

GET /regression/reports

Return recent regression reports.

6. Add endpoint:

GET /regression/reports/{report_id}

Return full report.

7. Add helper endpoint:

POST /regression/detect-latest

Request:

{
  "baseline_restore_point_id": "optional"
}

Behavior:
- Find latest two debug reports if no restore point is provided.
- Compare them.
- If restore point is provided, use debug report attached to restore point as baseline.
- Return clean error if not enough reports exist.

8. Timeline integration.

When regression report is created:
- Add change timeline event:
  event_type = "regression_report"
  status = "warning" if regressions found else "success"

Do not modify timeline architecture.

9. Frontend requirements.

Add Regression Detection section:

- Select baseline debug report
- Select current debug report
- Optional restore point selector
- Detect regressions button
- Detect latest button
- Show:
  - regressions
  - improvements
  - unchanged failures
  - likely files
  - suggested start point
- Link/display report id

Do not remove previous panels.

10. Success criteria:

1. All previous features still work.
2. Regression reports can be created from debug reports.
3. Latest two debug reports can be compared.
4. Regressions are clearly listed.
5. Improvements are clearly listed.
6. Timeline event is created.
7. No fixes/restores happen automatically.
8. Frontend exposes regression detection.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 56.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 56 ONLY: Add Performance Optimization Agent.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not apply optimizations automatically.
Do not run terminal commands automatically.
Do not modify source files automatically.

Goal:
Analyze performance bottlenecks and suggest safe optimizations for:
- RAG retrieval
- chunking
- embeddings
- Qdrant indexing/search
- prompts/context size
- frontend rendering
- backend API latency
- Docker resource usage
- storage usage

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 55.
- If fixing an error, touch only the exact file related to that error.
- Performance agent must be read-only.
- It may suggest changes but must not apply them.
- Any optimization must go through coder → reviewer → apply workflow.
- Do not delete data.
- Do not modify Qdrant collections automatically.

Backend requirements:

1. Add SQLite table:

performance_reports:
- id TEXT PRIMARY KEY
- scope TEXT NOT NULL
- summary_json TEXT
- bottlenecks_json TEXT
- recommendations_json TEXT
- metrics_json TEXT
- created_at TEXT NOT NULL

Allowed scopes:
- full_system
- rag
- qdrant
- embeddings
- backend
- frontend
- docker
- storage

2. Add module:

backend/app/performance_agent.py

Functions:
- collect_performance_metrics(scope)
- analyze_rag_performance()
- analyze_qdrant_performance()
- analyze_embedding_performance()
- analyze_backend_performance()
- analyze_frontend_performance()
- analyze_storage_performance()
- generate_performance_recommendations(metrics)
- create_performance_report(scope)

3. Metrics to collect.

RAG:
- average retrieved chunks
- average context size if available
- rerank enabled/disabled
- parent retrieval usage if available
- recent RAG failures from agent_runs

Qdrant:
- collection info
- vector count
- indexed chunk count
- failed index count

Embeddings:
- embedded chunk count
- failed embedding count
- embedding model
- documents waiting for embeddings

Backend:
- failed agent runs
- failed ingestion jobs
- API health statuses

Frontend:
- no deep browser performance yet
- check frontend build/static health if available

Storage:
- uploads size
- extracted size
- Qdrant size if accessible
- backups size
- debug reports size
- screenshots size

Docker:
- only check config/readiness
- do not run docker stats unless already safely supported

4. Add endpoint:

POST /performance/analyze

Request:

{
  "scope": "full_system"
}

Response:

{
  "status": "ok",
  "report_id": "...",
  "scope": "full_system",
  "summary": {
    "overall_performance": "good|warning|poor"
  },
  "bottlenecks": [
    {
      "area": "rag",
      "severity": "medium",
      "problem": "Too many chunks are being sent to model.",
      "evidence": "...",
      "likely_files": ["backend/app/rag.py"]
    }
  ],
  "recommendations": [
    {
      "title": "Reduce default RAG top_k",
      "risk": "low",
      "expected_benefit": "lower latency",
      "suggested_phase": "small patch"
    }
  ]
}

5. Add endpoint:

GET /performance/reports

Return recent reports.

6. Add endpoint:

GET /performance/reports/{report_id}

Return full report.

7. Agent integration.

Do not auto-send recommendations to coder.

But response should include "agent_fix_prompt" for each recommendation:

{
  "agent_fix_prompt": "Optimize only backend/app/rag.py to reduce context size..."
}

8. Frontend requirements.

Add Performance section:

- Scope dropdown
- Analyze button
- Show:
  - overall status
  - bottlenecks
  - evidence
  - likely files
  - recommendations
  - agent fix prompts
- Copy fix prompt button if easy

Do not remove existing panels.

Success criteria:

1. All previous features still work.
2. /performance/analyze works.
3. Performance reports are saved.
4. Bottlenecks include likely files.
5. Recommendations are read-only.
6. Agent fix prompts are generated.
7. No optimizations are applied automatically.
8. Frontend exposes performance analysis.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 57.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 57 ONLY: Add Knowledge Gap Detection.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not download documents automatically.
Do not scrape websites automatically.
Do not modify the knowledge base automatically.
Do not apply fixes automatically.

Goal:
Detect where the local knowledge base is weak, missing, outdated, or not useful enough for user questions.

Examples:
- User asks about Next.js App Router but no docs exist.
- RAG repeatedly returns weak sources.
- Many answers say "local knowledge is insufficient."
- Search results have low scores.
- A project has code but no architecture notes.
- A feature has no tests or docs.

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 56.
- If fixing an error, touch only the exact file related to that error.
- Gap detection must be read-only.
- Do not ingest new content automatically.
- Do not web search automatically unless user explicitly requests it later.
- Do not delete or rewrite existing knowledge.
- Do not expose secrets or full private document text.

Backend requirements:

1. Add SQLite table:

knowledge_gap_reports:
- id TEXT PRIMARY KEY
- scope TEXT NOT NULL
- summary_json TEXT
- gaps_json TEXT
- recommendations_json TEXT
- evidence_json TEXT
- created_at TEXT NOT NULL

Allowed scopes:
- full_system
- rag
- codebase
- knowledge_base
- project_docs
- failed_queries

2. Add module:

backend/app/knowledge_gap_detection.py

Functions:
- collect_gap_signals(scope)
- analyze_failed_rag_queries()
- analyze_low_score_searches()
- analyze_document_coverage()
- analyze_codebase_documentation_gaps()
- analyze_eval_failures()
- generate_gap_recommendations(signals)
- create_knowledge_gap_report(scope)

3. Signals to collect.

Use existing local data only:

From RAG/chat:
- questions that returned insufficient knowledge
- low source count
- low score_threshold failures
- missing citations warnings

From search:
- searches with zero results
- searches with weak scores

From RAG eval:
- failed eval questions
- weak source matches

From codebase indexing:
- source files with no related manual notes/docs
- important backend modules without documentation
- frontend panels without docs

From documents:
- knowledge bases with very few documents
- documents uploaded but not indexed
- documents with extraction failure
- documents with OCR recommended but not completed

4. Add endpoint:

POST /knowledge-gaps/analyze

Request:

{
  "scope": "full_system",
  "knowledge_base_id": "optional"
}

Response:

{
  "status": "ok",
  "report_id": "...",
  "scope": "full_system",
  "summary": {
    "gap_count": 0,
    "high_priority": 0,
    "medium_priority": 0,
    "low_priority": 0
  },
  "gaps": [
    {
      "id": "...",
      "priority": "high|medium|low",
      "area": "rag|docs|codebase|pdfs|tests",
      "problem": "Many RAG questions about authentication return weak sources.",
      "evidence": [],
      "recommended_action": "Add project authentication notes or index backend auth files.",
      "suggested_documents": [
        "manual note: authentication architecture",
        "project codebase files: backend/app/auth.py"
      ]
    }
  ],
  "recommendations": []
}

5. Add endpoint:

GET /knowledge-gaps/reports

Return recent reports.

6. Add endpoint:

GET /knowledge-gaps/reports/{report_id}

Return full report.

7. Optional prompt generation.

For each gap, include:

{
  "manual_note_prompt": "Write a manual note explaining...",
  "web_search_query": "official docs query...",
  "agent_action_prompt": "Index these files..."
}

Do not run these prompts automatically.

8. Frontend requirements.

Add Knowledge Gap section:

- Scope dropdown
- Optional knowledge base selector
- Analyze gaps button
- Show:
  - summary
  - priority
  - problem
  - evidence
  - recommended action
  - suggested documents
  - manual note prompt
  - web search query
  - agent action prompt
- Copy prompts/buttons if easy

Do not remove previous panels.

Success criteria:

1. All previous features still work.
2. /knowledge-gaps/analyze works.
3. Gap reports are saved.
4. Reports use only local signals.
5. Recommendations do not auto-ingest or auto-search.
6. Frontend shows gaps and suggested next actions.
7. No documents/vectors/source files are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 58.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 58 ONLY: Add Autonomous Project Auditor.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not apply fixes automatically.
Do not run destructive commands.
Do not delete files.
Do not install packages automatically.

Goal:
Create a high-level auditor that combines existing systems into one read-only project audit.

It should use:
- architecture understanding
- impact analysis
- debug reports
- regression detection
- coverage analysis
- performance reports
- knowledge gap reports
- maintenance checks
- test history
- agent run history
- change timeline
- restore points

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 57.
- If fixing an error, touch only the exact file related to that error.
- Auditor must be read-only.
- Auditor may recommend fixes but must not apply them.
- Auditor may create reports but must not modify source files.
- Auditor must not expose secrets or full private document text.

Backend requirements:

1. Add SQLite table:

project_audit_reports:
- id TEXT PRIMARY KEY
- scope TEXT NOT NULL
- overall_status TEXT NOT NULL
- summary_json TEXT
- findings_json TEXT
- recommendations_json TEXT
- fix_queue_json TEXT
- created_at TEXT NOT NULL

Allowed scopes:
- full_project
- backend
- frontend
- rag
- agents
- knowledge_base
- safety
- performance

2. Add module:

backend/app/project_auditor.py

Functions:
- collect_audit_inputs(scope)
- analyze_system_health(inputs)
- analyze_safety_risks(inputs)
- analyze_code_quality_risks(inputs)
- analyze_rag_quality(inputs)
- analyze_agent_reliability(inputs)
- analyze_rollback_readiness(inputs)
- create_project_audit_report(scope)
- get_project_audit_report(report_id)
- list_project_audit_reports()

3. Audit categories.

Auditor should produce findings for:

- broken features
- regressions
- low coverage
- weak RAG retrieval
- missing knowledge
- failed ingestion jobs
- failed agent runs
- risky recent changes
- missing restore points
- high storage usage
- missing models
- unsafe config
- repeated errors
- performance bottlenecks

4. Finding format.

Each finding:

{
  "id": "...",
  "severity": "low|medium|high|critical",
  "category": "rag|agents|safety|performance|tests|docs|storage|models|frontend|backend",
  "title": "...",
  "problem": "...",
  "evidence": [],
  "likely_files": [],
  "recommended_action": "...",
  "safe_fix_prompt": "..."
}

5. Fix queue.

The auditor should create a safe fix queue:

{
  "priority": 1,
  "finding_id": "...",
  "task": "...",
  "likely_files": [],
  "recommended_agent": "coder|reviewer|test_generator|manual",
  "requires_review": true,
  "requires_backup": true
}

Do not execute the fix queue.

6. Add endpoint:

POST /audit/run

Request:

{
  "scope": "full_project"
}

Response:

{
  "status": "ok",
  "report_id": "...",
  "overall_status": "good|warning|poor|critical",
  "summary": {
    "critical": 0,
    "high": 0,
    "medium": 0,
    "low": 0
  },
  "findings": [],
  "fix_queue": []
}

7. Add endpoint:

GET /audit/reports

Return recent reports.

8. Add endpoint:

GET /audit/reports/{report_id}

Return full report.

9. Timeline integration.

When audit report is created:
- Add change timeline event:
  event_type = "project_audit"
  status = "success" if no high/critical issues else "warning"

10. Frontend requirements.

Add Project Auditor section:

- Scope dropdown
- Run audit button
- Show overall status
- Show severity counts
- Show findings table
- Show evidence
- Show likely files
- Show safe fix prompts
- Show fix queue
- Copy fix prompt button

Do not remove previous panels.

Success criteria:

1. All previous features still work.
2. /audit/run creates read-only audit report.
3. Auditor combines existing reports/signals.
4. Findings include severity, evidence, likely files, and safe fix prompts.
5. Fix queue is generated but not executed.
6. Timeline event is created.
7. Frontend shows audit report.
8. No source files, documents, vectors, or settings are modified.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 59.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 59 ONLY: Add Audit-to-Fix Workflow.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not apply fixes automatically.
Do not run terminal commands automatically.
Do not skip reviewer approval.
Do not skip backups.

Goal:
Turn Project Auditor findings from Phase 58 into controlled repair tasks that flow through:

audit finding
→ fix task
→ impact analysis
→ coder draft
→ reviewer
→ dry-run apply
→ user confirm apply

IMPORTANT PROTECTION RULES:
- Preserve every feature from Phase 1 to Phase 58.
- If fixing an error, touch only the exact file related to that error.
- This workflow must orchestrate existing tools, not replace them.
- Fixes must never be applied automatically.
- Every fix must require review.
- Every apply must require confirm=true.
- Every apply must create backup.
- Do not modify source files in this phase except normal project implementation.

Backend requirements:

1. Add SQLite table:

audit_fix_tasks:
- id TEXT PRIMARY KEY
- audit_report_id TEXT NOT NULL
- finding_id TEXT NOT NULL
- title TEXT NOT NULL
- status TEXT NOT NULL
- priority INTEGER
- likely_files_json TEXT
- impact_report_id TEXT
- coder_result_json TEXT
- review_result_json TEXT
- apply_result_json TEXT
- created_at TEXT NOT NULL
- updated_at TEXT

Status values:
- queued
- impact_analyzed
- drafted
- reviewed
- ready_to_apply
- applied
- rejected
- failed

2. Add module:

backend/app/audit_fix_workflow.py

Functions:
- create_fix_tasks_from_audit(audit_report_id)
- get_fix_task(task_id)
- list_fix_tasks(audit_report_id=None, status=None)
- run_fix_task_impact_analysis(task_id)
- draft_fix_for_task(task_id)
- review_fix_task(task_id)
- dry_run_apply_fix_task(task_id)
- mark_fix_task_status(task_id, status)

3. Add endpoint:

POST /audit/reports/{audit_report_id}/create-fix-tasks

Behavior:
- Load audit report.
- Create one fix task per fix_queue item.
- Do not duplicate existing tasks for same report + finding.
- Do not run any fixes yet.

4. Add endpoints:

GET /audit/fix-tasks

Query:
- audit_report_id optional
- status optional

GET /audit/fix-tasks/{task_id}

POST /audit/fix-tasks/{task_id}/impact

Runs existing impact analysis and stores impact_report_id.

POST /audit/fix-tasks/{task_id}/draft

Uses existing coder agent to draft proposed changes.
Does not apply.

POST /audit/fix-tasks/{task_id}/review

Uses existing reviewer agent.
Does not apply.

POST /audit/fix-tasks/{task_id}/dry-run-apply

Uses existing apply endpoint behavior with dry_run=true.
Does not write files.

5. Do not add final apply endpoint yet.

Actual apply should still use existing /agent/apply manually with confirm=true.

This phase prepares and validates fixes only.

6. Timeline integration.

Create timeline events for:
- fix tasks created
- impact analysis completed
- draft completed
- review completed
- dry-run completed

7. Frontend requirements.

Add Audit Fix Workflow section:

- Select audit report
- Create fix tasks button
- List fix tasks
- For each task show:
  - status
  - priority
  - title
  - likely files
- Buttons:
  - Run impact
  - Draft fix
  - Review fix
  - Dry-run apply
- Show:
  - impact report id
  - coder result
  - review result
  - dry-run result

Do not add auto-fix-all.
Do not add final apply button in this phase.

Success criteria:

1. All previous features still work.
2. Audit report can generate fix tasks.
3. Fix tasks are not duplicated.
4. Impact analysis can run per task.
5. Coder draft can run per task.
6. Reviewer can run per task.
7. Dry-run apply can run per task.
8. No real apply happens automatically.
9. Timeline records workflow events.
10. Frontend exposes controlled workflow.

Before editing:
List exactly which files you will create or modify.

After editing:
Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to Phase 60.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE 61 ONLY: Add Multi-Project Workspace Manager.

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not modify projects automatically.
Do not run destructive commands.
Do not mix project data together.

Goal:

Allow the platform to discover, manage, analyze, test, and audit multiple local projects from a single dashboard.

Example:

/workspace/projects/

├── ProjectA
├── ProjectB
├── ProjectC

User can switch projects from UI and run:

- architecture scan
- codebase indexing
- tests
- audits
- regressions
- debug checks
- self-healing preparation
- RAG indexing

against the selected project.

IMPORTANT PROTECTION RULES:

- Preserve every feature from Phase 1 through Phase 60.
- Every project must be isolated.
- One project's vectors must not appear in another project.
- One project's documents must not appear in another project.
- One project's audits must not affect another.
- No automatic code modifications.
- No automatic Docker execution.

--------------------------------------------------
SECTION 1: PROJECT DISCOVERY
--------------------------------------------------

Add environment variable:

PROJECTS_ROOT=/workspace/projects

Scan:

PROJECTS_ROOT/*

Each folder becomes a project.

Detect:

- package.json
- requirements.txt
- pyproject.toml
- docker-compose.yml
- Dockerfile
- .git

Create project records.

--------------------------------------------------
SECTION 2: DATABASE
--------------------------------------------------

Add table:

projects

- id TEXT PRIMARY KEY
- name TEXT
- path TEXT UNIQUE
- detected_type TEXT
- status TEXT
- created_at TEXT

Add table:

project_sessions

- id TEXT PRIMARY KEY
- project_id TEXT
- last_opened_at TEXT
- metadata_json TEXT

--------------------------------------------------
SECTION 3: PROJECT ISOLATION
--------------------------------------------------

Every major system must support project_id.

Including:

- architecture
- indexing
- audits
- debug reports
- regressions
- tests
- timelines
- restore points
- self-healing

Never mix records.

--------------------------------------------------
SECTION 4: PROJECT SCAN
--------------------------------------------------

Add endpoint:

POST /projects/scan

Scan PROJECTS_ROOT.

Return:

{
  "projects_found": 12,
  "projects": [...]
}

--------------------------------------------------
SECTION 5: PROJECT OPEN
--------------------------------------------------

Add endpoint:

POST /projects/open

Request:

{
  "project_id": "..."
}

Set active project session.

Return:

{
  "active_project": {...}
}

--------------------------------------------------
SECTION 6: PROJECT ANALYSIS
--------------------------------------------------

Add endpoint:

GET /projects/{project_id}/summary

Return:

{
  "name": "...",
  "language": "...",
  "files": 0,
  "tests": 0,
  "coverage": 0,
  "audit_status": "...",
  "architecture_status": "...",
  "knowledge_base_status": "...",
  "last_debug_report": "...",
  "last_regression_report": "..."
}

--------------------------------------------------
SECTION 7: PROJECT TESTING
--------------------------------------------------

Add endpoint:

POST /projects/{project_id}/run-analysis

Request:

{
  "run_architecture": true,
  "run_audit": true,
  "run_debug": true,
  "run_regression": false,
  "run_codebase_indexing": true
}

Behavior:

Run selected systems only.

Store results under project_id.

--------------------------------------------------
SECTION 8: PROJECT DOCKER SUPPORT
--------------------------------------------------

Detect:

docker-compose.yml

Dockerfile

Status:

- available
- missing
- invalid

Add endpoint:

GET /projects/{project_id}/docker-status

Do not execute containers automatically.

Only detect.

--------------------------------------------------
SECTION 9: PROJECT KNOWLEDGE BASE
--------------------------------------------------

Each project gets:

project knowledge base

project vectors

project code index

project documents

All isolated.

--------------------------------------------------
SECTION 10: BULK ANALYSIS
--------------------------------------------------

Add endpoint:

POST /projects/run-bulk-analysis

Request:

{
  "project_ids": [...],
  "run_audit": true,
  "run_debug": true,
  "run_architecture": true
}

Behavior:

Queue jobs.

Run sequentially.

Do not run all simultaneously.

--------------------------------------------------
SECTION 11: FRONTEND
--------------------------------------------------

Add:

Projects Dashboard

Features:

- scan projects
- project list
- project search
- open project
- active project indicator

For each project:

Show:

- language
- framework
- test status
- audit status
- architecture status
- Docker status

Buttons:

- Architecture Scan
- Codebase Index
- Audit
- Debug
- Regression
- Self-Healing Prep

Bulk actions:

- Bulk Audit
- Bulk Debug
- Bulk Architecture Scan

--------------------------------------------------
SECTION 12: SUCCESS CRITERIA
--------------------------------------------------

1. All previous features still work.
2. Multiple projects can be discovered.
3. Project data remains isolated.
4. Architecture works per project.
5. Audit works per project.
6. Debug works per project.
7. Code indexing works per project.
8. Bulk analysis works.
9. Docker detection works.
10. Frontend exposes project management.

Before editing:

List exactly which files you will create or modify.

After editing:

Return:
- changed/new files
- run commands
- test checklist
- assumptions

Do not continue to another phase.
--------------------------------------------------------------------------------------------------------------------------
You are continuing the existing local AI coding assistant project.

PHASE OMEGA ONLY: Add Self-Optimization Engine.

This phase must be implemented ONLY after:
- architecture system is working
- impact analysis is working
- testing system is working
- regression detection is working
- project auditor is working
- RAG evaluation is working
- self-healing workflow is working
- backups/snapshots are working

Do not remove existing features.
Do not rewrite unrelated files.
Do not change existing API contracts unless backward compatible.
Do not apply optimizations automatically.
Do not modify prompts automatically.
Do not switch models automatically.
Do not rewrite code automatically.
Do not self-update automatically.

Goal:

Create a long-term learning system that observes how the platform performs over weeks and months.

This system does not fix things.

This system learns:

- what works
- what fails
- what causes regressions
- what improves performance
- what improves retrieval quality
- what improves coding quality

and then produces recommendations.

IMPORTANT PROTECTION RULES:

- Preserve every feature from Phase 1 through Phase Omega.
- This system is read-only.
- It may recommend.
- It may analyze.
- It may benchmark.
- It may compare.
- It must never apply changes itself.
- It must never execute fixes automatically.
- It must never bypass reviewer.
- It must never bypass backups.
- It must never alter user knowledge.

--------------------------------------------------
SECTION 1: LEARNING DATABASE
--------------------------------------------------

Create SQLite tables:

optimization_experiments
optimization_findings
optimization_metrics
optimization_recommendations

Track:

- model performance
- retrieval performance
- coding performance
- review performance
- test performance
- regression performance
- storage performance
- ingestion performance

--------------------------------------------------
SECTION 2: MODEL LEARNING
--------------------------------------------------

Track per model:

- tasks completed
- tasks failed
- review rejection rate
- average generation time
- average token usage
- average response length

Example:

qwen2.5-coder:7b

Store:

{
  "tasks_completed": 100,
  "review_pass_rate": 82,
  "avg_generation_time": 12.4
}

Detect:

- best backend model
- best frontend model
- best reviewer model
- best planner model

Do not switch models automatically.

Generate recommendations only.

--------------------------------------------------
SECTION 3: RAG LEARNING
--------------------------------------------------

Track:

- retrieval strategy used
- rerank usage
- chunk size
- overlap size
- parent retrieval usage
- citation quality
- evaluation score

Detect:

- best chunk size
- best overlap
- best retrieval strategy
- best rerank configuration

Do not modify configuration automatically.

--------------------------------------------------
SECTION 4: REGRESSION LEARNING
--------------------------------------------------

Track:

- regressions
- affected files
- failed repairs
- successful repairs

Detect:

High Risk Files

Example:

backend/app/rag.py

25 regressions

Mark:

{
  "risk_level": "high"
}

Future audits should reference this.

--------------------------------------------------
SECTION 5: TEST LEARNING
--------------------------------------------------

Track:

- test failures
- flaky tests
- most failing tests
- coverage trends

Detect:

- weak areas
- under-tested modules
- unstable modules

--------------------------------------------------
SECTION 6: KNOWLEDGE LEARNING
--------------------------------------------------

Track:

- knowledge gaps
- failed RAG evaluations
- unanswered questions

Detect:

Missing topics.

Example:

Authentication
OAuth
Docker Security

Recommend:

- documents
- notes
- code indexing

--------------------------------------------------
SECTION 7: PERFORMANCE LEARNING
--------------------------------------------------

Track trends:

- RAG latency
- embedding speed
- indexing speed
- retrieval speed
- frontend load times
- storage growth

Detect:

Performance degradation.

Example:

Retrieval:
2.1 sec
→
4.8 sec

Generate warning.

--------------------------------------------------
SECTION 8: EXPERIMENT SYSTEM
--------------------------------------------------

Allow recording experiments.

Example:

Experiment:

Chunk Size 1000

Result:

Eval Score 72%

Experiment:

Chunk Size 700

Result:

Eval Score 84%

Store comparison.

Recommend winner.

Never apply winner automatically.

--------------------------------------------------
SECTION 9: PROJECT MEMORY
--------------------------------------------------

Create long-term memory.

Store:

Successful Fixes

Example:

Issue:
Qdrant connection failure

Fix:
backend/app/qdrant.py

Result:
successful

Store pattern.

Future auditor may reference:

"Similar issue was previously fixed using this approach."

Do not auto-fix.

--------------------------------------------------
SECTION 10: OPTIMIZATION REPORT
--------------------------------------------------

Add endpoint:

POST /optimization/analyze

Generate:

{
  "overall_health": "...",
  "best_models": [],
  "best_retrieval_settings": [],
  "high_risk_files": [],
  "knowledge_gaps": [],
  "performance_trends": [],
  "successful_fix_patterns": [],
  "recommendations": []
}

--------------------------------------------------
SECTION 11: OPTIMIZATION SCORE
--------------------------------------------------

Generate:

Project Intelligence Score

Based on:

- retrieval quality
- test quality
- regression rate
- documentation quality
- coverage
- performance
- audit results

Example:

78/100

Show historical trend.

--------------------------------------------------
SECTION 12: FRONTEND
--------------------------------------------------

Add:

Self Optimization Dashboard

Show:

- intelligence score
- model rankings
- retrieval rankings
- performance trends
- high-risk files
- successful fixes
- failed fixes
- knowledge gaps
- recommendations

Do not add auto-fix button.

Do not add auto-optimize button.

--------------------------------------------------
SECTION 13: SUCCESS CRITERIA
--------------------------------------------------

1. All previous features still work.
2. No automatic modifications occur.
3. Optimization reports are generated.
4. Historical trends are stored.
5. Best configurations are identified.
6. Knowledge gaps are tracked.
7. Successful fixes are tracked.
8. High-risk files are detected.
9. Recommendations are produced.
10. Human remains in control.

Before editing:

List exactly which files you will create or modify.

After editing:

Return:
- changed/new files
- run commands
- test checklist
- assumptions

STOP AFTER PHASE OMEGA.
Do not continue beyond this phase.
--------------------------------------------------------------------------------------------------------------------------